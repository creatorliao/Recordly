# 09 · 执行留痕

## 变更 1 · P1 采集默认标准档

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/lib/capturePreset.ts`；录制偏好 `capturePreset`；HUD 三点菜单；`useScreenRecorder` 浏览器约束；`recording.ts` 原生 `fps`/`bitrateBps`；`wgc-capture` 可选码率 |
| **② 是怎么修改的** | 默认 standard=1080p30 / 10Mbps；economy / high 可选。原生不缩小纹理（见 `15`） |
| **③ 有什么作用** | 培训默认不再按 4K60 打满机器 |
| **④ 对工作流的影响** | 金路径按钮不变。三点菜单多「采集画质」。手册已提示 |

## 变更 2 · P2 重活让路

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 导出中一律 `suspendRendering`；相位 recording/mux/export/captions 向编辑器发 `preview-yield`；Whisper `-t` 限核；导出中拒绝生成字幕 |
| **② 是怎么修改的** | 不改导出算法，只停预览 ticker、限线程、互斥字幕 |
| **③ 有什么作用** | 减少「一边导一边播、一边识别」把整机卡死 |
| **④ 对工作流的影响** | 导出时预览静止；导出未完成点字幕会提示等一下 |

## 变更 3 · P3 导出默认 Good

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `projectPersistence.ts` 缺省 `exportQuality`：`source` → `good` |
| **② 是怎么修改的** | 仅新工程/无字段时；已写 `source` 的工程不改 |
| **③ 有什么作用** | 新导出不再默认按源 4K 码率再压一轮 |
| **④ 对工作流的影响** | 导出菜单仍可选 Source。新片默认更小、更省 |

- 专题：`R20260906-12`

---

## 变更 4 · 停录进编辑器不再误停预览

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/sessionLogFormat.ts`（`shouldYieldPreviewForPhase`）；`sessionLog.ts`；`recording.ts` 停录/mux；`EditorShell` 注释；`sessionLogFormat.test.ts` |
| **② 是怎么修改的** | 预览让路只保留 `export`/`captions`。去掉 `recording`/`mux`。停录成功切到 `editor`；mux 无论成败 `finally` 切回 `editor` |
| **③ 有什么作用** | 修 P2 误伤：进编辑器点播放不再因 mux 相位把 ticker/视频按死，避免闪屏和声画播不了 |
| **④ 对工作流的影响** | 金路径恢复「停录就能回放」。导出/生成字幕时预览仍停。无新入口 |

- 回退点（改前冻结）：`00781b78`
- 问题：`10-问题_编辑器播放闪屏与音视频无法播放.md`
- 版本：`1.4.0-beta.7`

---

## 变更 5 · 版本 1.4.0-beta.7

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `package.json` / `package-lock.json` 版本 |
| **② 是怎么修改的** | `1.4.0-beta.6` → `1.4.0-beta.7`，与让路回收同一提交，便于和闪屏的 Beta 6 包区分 |
| **③ 有什么作用** | 下一份 Portable 文件名带 beta.7，不再和已确认带 yield 的 Beta 6 混用 |
| **④ 对工作流的影响** | 不改录→停→编→导。解压后仍双击目录里的 exe |

- 产物：`release-portable/recordly-portable-1.4.0-beta.7.zip`（约 685MB，2026-09-06 19:22）

---

## 变更 6 · 撤回 preview-yield，并修播放 pause 环

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/sessionLog.ts` / `sessionLogFormat.ts`（去掉 yield）；`EditorShell.tsx`（去掉 `recordingYield`）；`VideoPlayback.tsx`（纹理/镜头 effect 依赖、`allowPlaybackRef`、`loadedmetadata`）；`recording.ts` 注释；`sessionLogFormat.test.ts` |
| **② 是怎么修改的** | 会话相位不再广播 `preview-yield`。预览停转只听导出。挂纹理、复位镜头不再因布局函数换身份而 pause/拆精灵；已在播时 `loadedmetadata` 只补时长 |
| **③ 有什么作用** | beta.7 让路回收后仍闪、没声、进度走不动。对准「点播放立刻被 pause」而不是再猜 mux 相位 |
| **④ 对工作流的影响** | 不改录→停→编→导步骤。导出时预览仍停（原 `shouldSuspendPreviewRendering`）。无新入口 |

- 问题：`13-问题报告_编辑器播放闪屏无声进度卡住.md`（底稿 `10` §8–§9）
- 前一轮无效改动：变更 4（只收 recording/mux 让路）

---

## 变更 7 · 第 1 包 1.4.0-beta.8（只动编辑器，供逐步验收）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 版本 `1.4.0-beta.8`；验收卡 `14-逐步验收_编辑器播放.md`；产品改动仍是变更 6（撤 yield + pause 环） |
| **② 是怎么修改的** | 采集默认不改。打 Portable，文件名带 beta.8，与已证伪的 beta.7 分开 |
| **③ 有什么作用** | 你按验 1/2/3 回三格，再决定下一刀动编辑器还是采集 |
| **④ 对工作流的影响** | 不改录→停→编→导。解压到**新目录**再跑 |

- 产物：`release-portable/recordly-portable-1.4.0-beta.8.zip`

---

## 变更 8 · 回放金路径会话日志（辅助第 1 包验收）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新建 `previewPlaybackLog.ts`；`VideoPlayback` / `videoEventHandlers` / `useEditorPlaybackControls`；`recording.start-attempt` 带上采集档；`14` 验收卡加读日志表 |
| **② 是怎么修改的** | 只记 toggle/play/pause/guard/loadedmetadata/progress-check/suspend/texture-mount/media-error。同类前 12 条全记，之后 2 秒一条 |
| **③ 有什么作用** | 你验的时候用日志区分「编辑器 pause 环」和「30fps/10Mbps 成片解码」 |
| **④ 对工作流的影响** | 不改按钮。启动窗仍用「打开日志文件夹」。日志在 `%APPDATA%\electron\logs` 或 portable 的 userData/logs |

---

## 变更 9 · beta.8 日志对账后修 Pixi 拆画布 + helper 路径

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `useProjectState.ts`（`setError` 稳定）；`VideoPlayback.tsx`（Pixi 只挂一次）；`electron/ipc/paths/binaries.ts` + 单测；验收卡 `14` |
| **② 是怎么修改的** | 日志：`setError` 换身份 → Pixi effect 拆画布；`isPackaged=false` → spawn 进 asar。对上后改这两处。让路/pause 环不撤 |
| **③ 有什么作用** | 停录回放不再因渲染把画布拆掉；Portable 应能起本机 WGC，不再被迫浏览器采集 |
| **④ 对工作流的影响** | 不改录→停→编→导。旧 beta.8 目录不会自己好 |

- 日志真源：`%APPDATA%\Electron\logs\recordly-20260906-200412-pid12656.log`

---

## 变更 10 · 拆提交并打 1.4.0-beta.9 Portable

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 四笔产品提交 + 本条留痕。未提交根目录 `preload.mjs`（Vite 残留） |
| **② 是怎么修改的** | `f33b7bd2` helper 路径；`72632d8e` Pixi/`setError`/撤 yield/会话日志；`18d353de` 报告与验收卡；`1af7176c` 版本 beta.9 + 本机构建重编 helper |
| **③ 有什么作用** | 验收包与提交树对齐；helper 在 `win-unpacked/resources/app.asar.unpacked/...` |
| **④ 对工作流的影响** | 不改录→停→编→导。解压**新目录**再跑，不要覆盖 beta.8 |

- 产物：`D:\code\Recordly\release-portable\recordly-portable-1.4.0-beta.9.zip`
