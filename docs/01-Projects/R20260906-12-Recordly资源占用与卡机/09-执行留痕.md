# 09 · 执行留痕

## 变更 1 · P1 采集默认标准档

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/lib/capturePreset.ts`；录制偏好 `capturePreset`；HUD 三点菜单；`useScreenRecorder` 浏览器约束；`recording.ts` 原生 `fps`/`bitrateBps`；`wgc-capture` 可选码率 |
| **② 是怎么修改的** | 默认 standard=1080p30 / 10Mbps；economy / high 可选。原生不缩小纹理（见 `15`） |
| **③ 有什么作用** | 培训默认不再按 4K60 打满机器 |
| **④ 对工作流的影响** | 金路径按钮不变。三点菜单多「采集画质」。手册已提示 |

## 变更 2 · P2 重活让路

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 导出中一律 `suspendRendering`；相位 recording/mux/export/captions 向编辑器发 `preview-yield`；Whisper `-t` 限核；导出中拒绝生成字幕 |
| **② 是怎么修改的** | 不改导出算法，只停预览 ticker、限线程、互斥字幕 |
| **③ 有什么作用** | 减少「一边导一边播、一边识别」把整机卡死 |
| **④ 对工作流的影响** | 导出时预览静止；导出未完成点字幕会提示等一下 |

## 变更 3 · P3 导出默认 Good

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `projectPersistence.ts` 缺省 `exportQuality`：`source` → `good` |
| **② 是怎么修改的** | 仅新工程/无字段时；已写 `source` 的工程不改 |
| **③ 有什么作用** | 新导出不再默认按源 4K 码率再压一轮 |
| **④ 对工作流的影响** | 导出菜单仍可选 Source。新片默认更小、更省 |

- 专题：`R20260906-12`

---

## 变更 4 · 停录进编辑器不再误停预览

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/sessionLogFormat.ts`（`shouldYieldPreviewForPhase`）；`sessionLog.ts`；`recording.ts` 停录/mux；`EditorShell` 注释；`sessionLogFormat.test.ts` |
| **② 是怎么修改的** | 预览让路只保留 `export`/`captions`。去掉 `recording`/`mux`。停录成功切到 `editor`；mux 无论成败 `finally` 切回 `editor` |
| **③ 有什么作用** | 修 P2 误伤：进编辑器点播放不再因 mux 相位把 ticker/视频按死，避免闪屏和声画播不了 |
| **④ 对工作流的影响** | 金路径恢复「停录就能回放」。导出/生成字幕时预览仍停。无新入口 |

- 回退点（改前冻结）：`00781b78`
- 问题：`10-问题_编辑器播放闪屏与音视频无法播放.md`
- 版本：`1.4.0-beta.7`

---

## 变更 5 · 版本 1.4.0-beta.7

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `package.json` / `package-lock.json` 版本 |
| **② 是怎么修改的** | `1.4.0-beta.6` → `1.4.0-beta.7`，与让路回收同一提交，便于和闪屏的 Beta 6 包区分 |
| **③ 有什么作用** | 下一份 Portable 文件名带 beta.7，不再和已确认带 yield 的 Beta 6 混用 |
| **④ 对工作流的影响** | 不改录→停→编→导。解压后仍双击目录里的 exe |

- 产物：`release-portable/recordly-portable-1.4.0-beta.7.zip`（约 685MB，2026-09-06 19:22）
