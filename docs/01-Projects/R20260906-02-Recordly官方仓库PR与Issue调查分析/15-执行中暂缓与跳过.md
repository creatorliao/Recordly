# 15 · 执行中暂缓与跳过（你回来先看这份）

> **规则**：某个 PR 综合考量后**实在过不去**，且**不是「缺你一句话才能决定」**（决策得出来：就是暂缓/跳过），则：  
> 1）先在本文追加一条完整记录；  
> 2）状态标 **暂缓** 或 **跳过**；  
> 3）**立刻做队列里下一条该合的**，不中断、不问。  
> 执行全部结束后，你回来看本文 + [`09`](09-执行留痕.md) 对账，再按 [`16-人工验收清单.md`](16-人工验收清单.md) 抽查。`16` 里暂缓/跳过的行标免验。

与 [`12` §十](12-综合完善方案.md) 、[`14`](14-待确认代决策与待检验.md) 分工：

| 文件 | 看什么 |
|------|--------|
| **本文 `15`** | 执行时**没合进去**的 PR：过不去、暂缓、跳过 |
| `09` | **合进去了**的改动（四要素 + 平衡卡） |
| `14` §二 | 事先代决策，等你勾 |
| `14` §三 | 合进去了或环境问题，但要你**真机检验** |

---

## 一、什么叫「过不去」且「决策过得去」

**过不去（写入本文，不合）：**

- 四闸任一门不过（范围 / 落不进 G / 六问不过 / 体量禁忌）
- 象限是「高价值高风险」或「不碰」，且本轮没有「拆小后再合」的现成 diff
- cherry-pick / 一次 rebase 仍冲突
- 合上后 `npm test` / lint / typecheck 红，按 D3 revert
- diff 与当前树对不上（上游已修或 PR 过期）
- 平衡矩阵筛完不进极高分，也不在本轮队列里硬插

**决策过得去（因此不必问你）：**  
上面这些，方案里已经规定动作 = **暂缓并跳下一条**。没有「既不能合又不能跳、必须等人」的空档。  
只有 D11（连续两条金路径都 revert）才暂停整轨，仍先写本文再停。

**不要写进本文的：** 需要你事后勾的代决策（去 `14` §二）；合成功但要人眼看的（去 `14` §三）。

---

## 二、每条必须写全（否则你回来看不懂）

追加时用下面这块，**不准只写「#xxx 失败」**。

```
### S-__  ·  #<号>  <标题一句话>
- 时间：
- 队列位置：上一条做成了 #… / 下一条将做 #…
- 动作：暂缓 | 跳过（本轮不再重试）
- 过不去的点：（闸门 / 六问 / 冲突 / 测试红 / 对不上树 / 其他）
- 综合考量：R/C/E/S 或「未打分（闸门已杀）」；象限；为什么不能硬合
- 试过什么：cherry-pick / rebase 一次 / 测试命令与结果
- 仓库状态：已 revert / 从未合上 / 工作树干净
- 你回来可以怎么处理：下轮再取 / 改手工 / 认定不做
```

---

## 三、本轮队列对照（执行时勾）

开工前预置，做完或跳过就改状态。你回来先扫这张表。

| 序 | PR | 计划 | 结果（执行后填） | 详见 |
|----|----|------|------------------|------|
| 0 | #823 / #837 | 已合 | 已合 `10596ded` | `09` |
| 1 | #542 | 合 | 跳过（见 S-01） | S-01 |
| 2 | #730 | 合 | 跳过（见 S-02） | S-02 |
| 3 | #855 | 合 | 已合 `d8fe5eba` | `09` 变更 3 |
| 4 | #655 | 合（D1） | 跳过（见 S-03） | S-03 |
| 5 | #671 | 合（D1） | 已合 `8590ca40` | `09` 变更 4 |
| 6 | #803 | 合；失败则 revert | 跳过（见 S-04） | S-04 |
| 7 | #785 | 合；麦测不挡 | **已合** `0a5dd9f5`（手工移植） | `09` 变更 5 |
| 8 | #796 | 合 + 手册 | **已合** `2c0263c0`+`5a575f10`+`aa67fb78`（手工移植+手册） | `09` 变更 6 |

结果只能是：`已合` / `暂缓（见 S-xx）` / `跳过（见 S-xx）` / `未开工`。

---

## 四、暂缓 / 跳过流水（执行中追加，时间倒序）

> 开工后首轮执行：合 #855/#671 两条；#542/#730/#655/#803 已被上游维护者等效修复（跳过）；#785/#796 冲突（暂缓）。
> **第二轮（手工移植）**：#785、#796 已逐文件手工移植并合入（见 `09` 变更 5/6），S-05/S-06 下面的「暂缓」改为「已合」，原记录保留供回查。

### S-06 · #796 编辑器返回录制 + 项目菜单
- 时间：2026-09-06（首轮暂缓 → 第二轮手工移植已合）
- 动作：**已合**（手工移植，见 `09` 变更 6）
- 过不去的点：cherry-pick 冲突，且冲突面大。`VideoEditor.tsx` 当前树已拆分为多个 domain-state hooks（`useEditorExportController`、`useEditorSettingsPanelProps`、`useShortcuts` 等），PR 的 172 行 UI 变更基于旧单体组件结构，无法直接映射；11 个 locale 的 `editor.json` 的 `project` 块两侧各自新增了不同 key（当前树已有 `unsavedChangesTitle/…/saveProject`，PR 新增 `menu/newRecording/…/saveAs`）
- 综合考量：体量 +319 行/16 文件、动编辑器核心 UI、**会改变「退出才能回去」的操作方式**（R14 必须改手册）；冲突解完易「改味」（T9）。D2 规定冲突不手搓
- 试过什么：`git cherry-pick 81a8a45e` → VideoEditor.tsx + 11 个 locale 全部 CONFLICT；已 `--abort`
- 仓库状态：已 revert / 工作树干净
- 你回来可以怎么处理：手工按当前 hooks 结构把「返回录制」按钮 + 项目菜单 dropdown 移植进 `VideoEditor.tsx`（基建 `closeEditorWindowBypassingUnsavedPrompt`、`saveProjectFile`、`openProjectsDirectory` 已在树内），只保留 zh-CN + en 文案，同步更新 `R20260906-01/02` + 速查卡变更提示

### S-05 · #785 Windows 麦克风 sidecar 原子发布
- 时间：2026-09-06（首轮暂缓 → 第二轮手工移植已合）
- 动作：**已合**（手工移植，见 `09` 变更 5）
- 过不去的点：cherry-pick 冲突，3 个文件：`electron/ipc/register/recording.ts`（HEAD 新增了 HUD 捕获保护相关 import，PR 新增 `INCOMPLETE_SIDECAR_SUFFIX` import，两侧 import 不同需合并）、`src/hooks/useScreenRecorder.ts`（HEAD 已把 mux finalization 重构成 `Promise.allSettled([webcam, microphone, mux])`，PR 的 try/catch 写法是旧结构）、`useSourceAudioFallback.ts`（HEAD 用 tab 缩进，PR 用空格，属格式化差异 + PR 新增 recheck 逻辑）
- 综合考量：G1+G3 核心（麦 15–20s 截断），但动音频 sidecar 链路 + 冲突需逐文件判断新旧语义映射。D2 规定冲突不手搓
- 试过什么：`git cherry-pick 06810e2f` → 3 文件 CONFLICT；已 `--abort`
- 仓库状态：已 revert / 工作树干净
- 你回来可以怎么处理：手工移植——`recording.ts` 合并 import 并按当前结构加 `markCompanionAudioSidecarPending`/staging 写+原子 rename；`useScreenRecorder.ts` 在现有 `Promise.allSettled` 结构里保留「mux 失败不抑制广播」语义；`useSourceAudioFallback.ts` 按 tab 缩进补 recheck 逻辑

### S-04 · #803 HUD 透明区域点击穿透
- 时间：2026-09-06
- 队列位置：上一条做了 #671 / 下一条将做 #785
- 动作：跳过（本轮不再重试；已被上游等效覆盖）
- 过不去的点：当前树已含等效实现——`electron/hudOverlayBounds.ts`（`getHudOverlayWindowBounds`/`shouldExpandHudOverlayFallback`/`resizeHudOverlayFallbackBounds`）、`src/components/launch/hudMousePassthrough.ts`（`mergeHudInteractiveBounds`/`shouldRestoreHudMousePassthroughAfterDrag`）、`windows.ts` 的 `setIgnoreMouseEvents(true, { forward: true })` + `reassertHudOverlayCaptureProtection()`；另有上游维护者提交 `ae36ac8a`/`7899d8fc`（HUD capture protection，2026-09-03）
- 综合考量：PR 的目标「透明区点穿、可见控件可点」已由维护者实现得更完整（S≈4.2 但对象已不存在）
- 试过什么：符号/文件搜索确认 `hudMousePassthrough.ts`、`hudOverlayBounds.ts` 已在树内且功能完整
- 仓库状态：未合 / 工作树干净
- 你回来可以怎么处理：真机走 A-803（点被录软件底部按钮 + HUD 可见控件仍可点）；若有残缺点穿再回头找小 PR

### S-03 · #655 4K「Original」导出用真实源尺寸
- 时间：2026-09-06
- 队列位置：上一条做了 #855 / 下一条将做 #671
- 动作：跳过（本轮不再重试；已被上游等效覆盖）
- 过不去的点：PR 改的 `VideoEditor.tsx` 里 `desiredGifOutputDimensions`/`desiredMp4SourceDimensions` 两个 useMemo 已不存在；导出尺寸逻辑已重构进 `src/components/video-editor/export/useExportDimensions.ts`，其中 `sourceDimensions` memo 已依赖 `isPreviewReady` 并读 `videoWidth/videoHeight`（正是 PR 要修的点，且更彻底）
- 综合考量：S≈3.8（顺手）；对象已被重构覆盖，无合入意义
- 试过什么：`grep -n "desiredGifOutputDimensions"` 无命中；`useExportDimensions.ts` 确认 `isPreviewReady` 已在依赖数组
- 仓库状态：未合 / 工作树干净
- 你回来可以怎么处理：有 4K 源时走 A-S5 验「Original」导出尺寸；正常即认定已覆盖

### S-02 · #730 HUD 拖顶消失 / 无法重定位 / 录制中消失
- 时间：2026-09-06
- 队列位置：上一条做了 #542 判定（跳过）/ 下一条将做 #855
- 动作：跳过（本轮不再重试；已被上游等效覆盖）
- 过不去的点：PR 基于旧 HUD 拖拽结构（merge-base `641d2230`）。当前树 `useHudBarDrag.ts` 已重构为「原生拖拽 + `clampHudOffsetToViewport` + `mergeHudInteractiveBounds`」，且上游维护者已合 `dba5e350 fix(hud): keep controls visible when recording starts`、`fcccd1de`、`316c1103` 等同类修复；PR 的 `recording` 重置 offset / `setSkipTaskbar` / 删 `applyHudOverlayBounds()` 三处改动无法直接映射到新结构
- 综合考量：目标（拖顶不消失、可重定位、录中不挡交互）已被更完整实现覆盖
- 试过什么：逐文件比对 `useHudBarDrag.ts`/`windows.ts`/`LaunchWindow.tsx` 现状与 PR diff
- 仓库状态：未合 / 工作树干净
- 你回来可以怎么处理：真机走 A-S4（HUD 拖到顶再录，条还在、能拖回）；有残缺点再找小 PR

### S-01 · #542 HUD 拖动后恢复原位点击穿透
- 时间：2026-09-06
- 队列位置：轨道 A 首条 / 下一条将做 #730
- 动作：跳过（本轮不再重试；已被上游等效覆盖）
- 过不去的点：`git cherry-pick 936fcf4a` CONFLICT。当前树 `LaunchWindow.tsx` 已把 `pointer-events-auto` + 鼠标事件挂在变换后的 `hudBarRef`（`motion.div`）上、外层已是 `pointer-events-none`——正是 PR 想达成的「穿透跟随拖动后的位置」，且更精确
- 综合考量：S≈4.15 但对象已不存在；等效修复已在树内
- 试过什么：`git cherry-pick 936fcf4a` → CONFLICT（上下文已变）；`git cherry-pick --abort`；读 `LaunchWindow.tsx` 确认 `hudBarRef` 已有 `pointer-events-auto`
- 仓库状态：已 abort / 工作树干净
- 你回来可以怎么处理：真机走 A-S3（拖 HUD 后点被录窗口，仍能点穿）；通过即认定覆盖

---

## 五、执行全部结束后的摘要（AI 收工时填）

| 项 | 填 |
|----|----|
| 收工时间 | 2026-09-06（轨道 A 两轮完成） |
| 本轮已合 | #855 `d8fe5eba`、#671 `8590ca40`、#785 `0a5dd9f5`、#796 `2c0263c0`+`5a575f10`+`aa67fb78` |
| 本文新增暂缓/跳过条数 | 6（S-01～S-06；其中 S-05/S-06 已转手工移植合入） |
| 是否触发 D11 整轨暂停 | 否 |
| 请你优先看的 S- 编号 | S-01～S-04 为「上游已覆盖」，真机抽查即可；S-05/S-06 已合，真机走 A-785/A-796 |
