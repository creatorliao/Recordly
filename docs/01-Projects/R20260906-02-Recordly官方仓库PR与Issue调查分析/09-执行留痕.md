# 09 · 执行留痕（修改内容 · 方式 · 作用 · 工作流影响）

> 记录 `dev-creator` 分支上每一次代码变更的四个维度，作为可追溯的执行档案。
> 每一条变更对应：① 修改了哪些内容 ② 是怎么修改的 ③ 有什么作用 ④ 对原工作流程的影响（**若改变操作方式，需同步更新使用手册并提示用户调整操作行为**）。

---

## 变更 1 · 简体中文 locale 标签（来自 PR #823）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/launch/popovers/MorePopover.tsx` 与 `src/components/video-editor/SettingsPanel.tsx` 中语言映射表里的 `"zh-CN"` 条目 |
| **② 是怎么修改的** | 将标签值 `"簡體中文"`（繁体写法）改为 `"简体中文"`（简体写法），共 2 处 |
| **③ 有什么作用** | 语言选择器里「简体中文」用正确的简体字显示，不再误用繁体字形 |
| **④ 对工作流的影响** | 无功能性影响；纯显示文案修正，不改变录制/编辑/导出任何行为 |

- 提交：`10596ded` `fix(i18n,editor): 低垂果实 — 简体中文标签 + 裁剪 0.1% 精度`

---

## 变更 2 · 裁剪精度 1% → 0.1%（来自 PR #837）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/video-editor/SettingsPanel.tsx` 中裁剪（crop）相关的 4 个计算变量 + 4 个裁剪滑块 |
| **② 是怎么修改的** | ① 计算：`Math.round(crop.x * 100)` → `Math.round(crop.x * 1000) / 10`（top/left/bottom/right 共 4 处，保留 1 位小数）；② 滑块：`step={1}` → `step={0.1}`（4 处）；③ 显示：`formatValue` 由 `` `${Math.round(v)}%` `` 改为 `` `${(Math.round(v * 10) / 10).toFixed(1)}%` `` |
| **③ 有什么作用** | 裁剪步进从 1% 细化到 0.1%，能更精确地裁掉画面多余边缘 |
| **④ 对工作流的影响** | 裁剪滑块变细、可微调；纯数值精度变化，不影响导出管线、不影响已保存的 `.recordly` 项目（仍是 0–50 范围） |

- 提交：`10596ded`（与变更 1 同一次提交）

---

## 语言范围调整（决策记录）

> **范围收敛**：本地化只关注 **简体中文（zh-CN）** 与 **英文（en）**，不做波兰语/俄语/印尼语/阿拉伯语/繁中等额外修复。

据此从方案中**移除**以下语言类 PR：`#710`（9 语种补 key）、`#734`（波兰语）、`#691`（俄语）、`#664`（印尼语）、`#316`（阿拉伯语）、`#527`（繁中）。
保留：`#823`（已合）、`#726`（zh-CN 补全，标 Slop，需重写后再议）、`#595`（中文翻译补充，如需）。

---

## 人工验收清单（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增 `16-人工验收清单.md`；`12`/`14`/`15`/`AGENTS.md`/`00` 指向它 |
| **② 是怎么修改的** | 一份清单：走查顺序 + 总表（列关联 PR）+ 小项合并抽查 + #803/#785/#796/安装分节；不另拆 17 |
| **③ 有什么作用** | 你知道先对账再点哪里、怎样算过、和哪个 PR 对应 |
| **④ 对工作流的影响** | 无产品行为变化 |

## 暂缓跳过落盘（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增 `15-执行中暂缓与跳过.md`；`12`/`14`/`AGENTS.md`/`00` 规定「过不去先记 15 再跳」 |
| **② 是怎么修改的** | 与「缺决策」分开：能决定暂缓就不问人；每条必须写原因、试过什么、下一条是谁 |
| **③ 有什么作用** | 你收工后看 `15`+`09` 即知做成/没做成 |
| **④ 对工作流的影响** | 无产品行为变化 |

## 执行机制补强（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增 `14-待确认代决策与待检验.md`；`12` 增 §十；`AGENTS.md` / `00` 同步「执行不中断提问」 |
| **② 是怎么修改的** | 能自动的写进一条龙；要人拍的先代决（D1–D16）；真机/困惑进 T1–T11 |
| **③ 有什么作用** | 开工后不被确认弹窗打断；你回来只改一份表 |
| **④ 对工作流的影响** | 不改变录/编/导。真麦、点击边角、监控拦截仍留待你检验 |

## 拍板入档（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `12` §九改为已拍板；新增 `13-Windows构建与环境复盘.md`；`00`/`README`/`AGENTS.md` 同步；使用指南注明本仓 Windows 焦点与 Electron 身份 |
| **② 是怎么修改的** | 按你确认的「按推荐执行」五条写死；复盘只写仓库里能核实的提交与命令，不编造未导出的报错 |
| **③ 有什么作用** | 决策可回看；下次装环境有最短路径；指南不误导「必须改成只写 Windows」 |
| **④ 对工作流的影响** | 不改变录/编/导。安装包窗口可能叫 Electron，已在 `02` 写明 |

## 方案完善（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增/重写 `12-综合完善方案.md`；对齐 `00`/`10`/`11`/`README`/`AGENTS.md`；给 `02`–`08` 加过时声明 |
| **② 是怎么修改的** | 以工作区 2 场 PI（54 条）为诉求真源，消解旧稿自相矛盾，补上安装包轨道与漏评的 Windows PR |
| **③ 有什么作用** | 执行口径只剩一份；确认前不再改产品代码 |
| **④ 对工作流的影响** | 不改变录制/编辑/导出；等确认后再按 `12` §5.4 动手 |

## 变更 3 · VFR 预览时钟冻结修复（来自 PR #855）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/video-editor/videoPlayback/videoEventHandlers.ts` 与 `videoEventHandlers.test.ts` |
| **② 是怎么修改的** | 移除 `requestVideoFrameCallback`（rVFC）驱动预览时钟的逻辑（`PresentedFrameMetadata`/`PresentedFrameVideoElement` 类型、`videoFrameRequestId`、`getPresentedTime`），改为统一用 `requestAnimationFrame` + `video.currentTime` 驱动 `updateTime()` |
| **③ 有什么作用** | 修复 VFR（变帧率）素材在编辑器预览时时钟冻结/不前进的问题（#854） |
| **④ 对工作流的影响** | 仅编辑器预览时钟驱动方式变化；不改变录→停→编→导流程，无新入口、无默认行为变化 |

- 提交：`d8fe5eba` `fix: keep preview clock moving between VFR frames`

```
PR #855  标题：fix: keep preview clock moving between VFR frames
闸门：范✓ 标✓ 六✓ 体✓
R=4  C=4  E=4  S=4.00
象限：优先（R 过线）
低垂果实？风险低✓ 效果好✓ 修复简单✓
默认行为改变？否
集群：编辑器预览  角色：主方案
结论：已合（cherry-pick 干净，测试/tsc/lint 全绿）
一句话理由：编得动（G1+G2）；删 rVFC 换 rAF，体量小且无回归
```

---

## 变更 4 · 播放头秒数进位到分钟（来自 PR #671）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/video-editor/timeline/core/time.ts` 与 `time.test.ts` |
| **② 是怎么修改的** | `formatPlayheadTime` 由「先除 60 取分/秒」改为「先按 0.1 秒精度四舍五入（tenths），再 `Math.floor(tenths/600)` 取分、`(tenths - min*600)/10` 取秒」；补测试 |
| **③ 有什么作用** | 59.97s 不再渲染成「60.0」这类不进位的怪值，正确滚动到下一分钟 |
| **④ 对工作流的影响** | 纯时间线显示修正；不改变录/编/导任何行为 |

- 提交：`8590ca40` `fix(timeline): roll playhead seconds into the next minute [G4] (#671)`

```
PR #671  标题：fix(timeline): roll playhead seconds into the next minute
闸门：范✓ 标✓ 六✓ 体✓
R=4  C=3  E=2  S=3.20
象限：顺手
低垂果实？风险低✓ 效果好□ 修复简单✓   （效果好偏弱）
默认行为改变？否
集群：时间线显示  角色：主方案
结论：已合（cherry-pick 干净，测试/tsc/lint 全绿）
一句话理由：体验项（G4 弱）；体量极小、无回归，按 D1 顺手合
```

---

## 变更 5 · Windows 麦克风 sidecar 原子发布（来自 PR #785）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/ipc/constants.ts`、`recording/diagnostics.ts`（+测试）、`recording/prune.ts`、`register/recording.ts`、`utils.ts`、`electron-env.d.ts`、`src/.../audio/useSourceAudioFallback.ts`（8 文件） |
| **② 是怎么修改的** | 手动移植（原 cherry-pick 冲突）：FFmpeg 麦 sidecar 改写到 `.incomplete` 暂存路径→`moveFileWithOverwrite` 原子 rename 发布；新增 `isFinalizedWavFile`（检测 0xFFFFFFFF 未封装的 WAV 头）；主进程维护 `pendingCompanionAudioSidecars` 集合，`get-video-audio-fallback-paths` 返回 `pending`，编辑器据 pending 定时重查；`useScreenRecorder.ts` 未改（现树已用 `Promise.allSettled` 实现同语义） |
| **③ 有什么作用** | 修复 Windows 麦克风 15–20s 后截断：编辑器不再提前读到「看似合法却截断」的未封装 WAV（G1 听得清 + G3） |
| **④ 对工作流的影响** | 不改变录/编/导；副作用是 sidecar 发布稍晚几秒，编辑器自动重查补齐 |

- 提交：`0a5dd9f5` `fix(recording): publish Windows mic sidecar atomically [G1][G3] (#785)`

```
PR #785  标题：fix(recording): publish Windows mic sidecar atomically
闸门：范✓ 标✓ 六✓ 体✓
R=3  C=5  E=5  S=4.20
象限：优先（R 刚过线）
低垂果实？风险低□ 效果好✓ 修复简单□   （动音频链路，不低垂）
默认行为改变？否（内部实现变化）
集群：录音可靠性  角色：主方案
结论：已合（手工移植 + isFinalizedWavFile 测试，120 测试文件全绿）
一句话理由：金路径「听得清」硬修复；冲突已按当前结构逐文件重写
```

---

## 变更 6 · 编辑器返回录制 + 项目菜单（来自 PR #796）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/main.ts`、`preload.ts`、`electron-env.d.ts`、`src/lib/shortcuts.ts`、11 个 locale `editor.json`、`project/useProjectOpenActions.ts`、`useEditorProjectController.ts`、`layout/EditorHeader.tsx`、`EditorShell.tsx`、`VideoEditor.tsx` |
| **② 是怎么修改的** | 手动移植（原 cherry-pick 冲突：VideoEditor 已拆分为 EditorHeader/EditorShell + project hooks）。拆两个提交：① 基础设施（`switch-to-recording` IPC 复用 `closeEditorWindowToHud` + preload 桥接 + FIXED_SHORTCUTS 加 Ctrl+N/S/Shift+S/O + 11 locale 文案）；② UI（header 相机按钮「返回录制」+「项目」下拉菜单，错误屏加按钮，`handleReturnToRecording` 落在 useProjectOpenActions 复用未保存确认对话框） |
| **③ 有什么作用** | Windows 从此**能保存/打开项目**（之前 `Menu.setApplicationMenu(null)` 导致无入口，G1 存得了）；新增显式「返回录制」入口，不必杀进程 |
| **④ 对工作流的影响** | **新增入口、不拆旧习惯**：关编辑器窗口仍可退回录制；保存/打开多了菜单和快捷键 |

- 提交：`2c0263c0`（基础设施）+ `5a575f10`（UI）+ `aa67fb78`（手册同步）

```
PR #796  标题：feat(editor): return-to-recording action and project menu
闸门：范✓ 标✓ 六✓ 体✓
R=3  C=5  E=4  S=4.15
象限：优先
低垂果实？风险低□ 效果好✓ 修复简单□
默认行为改变？是（新增入口，默认仍可关窗口）→ 手册：已同步 R20260906-01/02+05
集群：编辑器入口  角色：主方案
结论：已合（手工移植，typecheck/lint/120 测试文件全绿）
一句话理由：金路径「存得了」硬修复；改操作已按 R14 同步手册
```
