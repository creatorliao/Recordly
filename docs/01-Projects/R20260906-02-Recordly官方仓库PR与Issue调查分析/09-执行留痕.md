# 09 · 执行留痕（修改内容 · 方式 · 作用 · 工作流影响）

> 记录 `dev-creator` 分支上每一次代码变更的四个维度，作为可追溯的执行档案。
> 每一条变更对应：① 修改了哪些内容 ② 是怎么修改的 ③ 有什么作用 ④ 对原工作流程的影响（**若改变操作方式，需同步更新使用手册并提示用户调整操作行为**）。

---

## 变更 1 · 简体中文 locale 标签（来自 PR #823）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/launch/popovers/MorePopover.tsx` 与 `src/components/video-editor/SettingsPanel.tsx` 中语言映射表里的 `"zh-CN"` 条目 |
| **② 是怎么修改的** | 将标签值 `"簡體中文"`（繁体写法）改为 `"简体中文"`（简体写法），共 2 处 |
| **③ 有什么作用** | 语言选择器里「简体中文」用正确的简体字显示，不再误用繁体字形 |
| **④ 对工作流的影响** | 无功能性影响；纯显示文案修正，不改变录制/编辑/导出任何行为 |

- 提交：`10596ded` `fix(i18n,editor): 低垂果实 — 简体中文标签 + 裁剪 0.1% 精度`

---

## 变更 2 · 裁剪精度 1% → 0.1%（来自 PR #837）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/video-editor/SettingsPanel.tsx` 中裁剪（crop）相关的 4 个计算变量 + 4 个裁剪滑块 |
| **② 是怎么修改的** | ① 计算：`Math.round(crop.x * 100)` → `Math.round(crop.x * 1000) / 10`（top/left/bottom/right 共 4 处，保留 1 位小数）；② 滑块：`step={1}` → `step={0.1}`（4 处）；③ 显示：`formatValue` 由 `` `${Math.round(v)}%` `` 改为 `` `${(Math.round(v * 10) / 10).toFixed(1)}%` `` |
| **③ 有什么作用** | 裁剪步进从 1% 细化到 0.1%，能更精确地裁掉画面多余边缘 |
| **④ 对工作流的影响** | 裁剪滑块变细、可微调；纯数值精度变化，不影响导出管线、不影响已保存的 `.recordly` 项目（仍是 0–50 范围） |

- 提交：`10596ded`（与变更 1 同一次提交）

---

## 语言范围调整（决策记录）

> **范围收敛**：本地化只关注 **简体中文（zh-CN）** 与 **英文（en）**，不做波兰语/俄语/印尼语/阿拉伯语/繁中等额外修复。

据此从方案中**移除**以下语言类 PR：`#710`（9 语种补 key）、`#734`（波兰语）、`#691`（俄语）、`#664`（印尼语）、`#316`（阿拉伯语）、`#527`（繁中）。
保留：`#823`（已合）、`#726`（zh-CN 补全，标 Slop，需重写后再议）、`#595`（中文翻译补充，如需）。

---

## 人工验收清单（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增 `16-人工验收清单.md`；`12`/`14`/`15`/`AGENTS.md`/`00` 指向它 |
| **② 是怎么修改的** | 一份清单：走查顺序 + 总表（列关联 PR）+ 小项合并抽查 + #803/#785/#796/安装分节；不另拆 17 |
| **③ 有什么作用** | 你知道先对账再点哪里、怎样算过、和哪个 PR 对应 |
| **④ 对工作流的影响** | 无产品行为变化 |

## 暂缓跳过落盘（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增 `15-执行中暂缓与跳过.md`；`12`/`14`/`AGENTS.md`/`00` 规定「过不去先记 15 再跳」 |
| **② 是怎么修改的** | 与「缺决策」分开：能决定暂缓就不问人；每条必须写原因、试过什么、下一条是谁 |
| **③ 有什么作用** | 你收工后看 `15`+`09` 即知做成/没做成 |
| **④ 对工作流的影响** | 无产品行为变化 |

## 执行机制补强（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增 `14-待确认代决策与待检验.md`；`12` 增 §十；`AGENTS.md` / `00` 同步「执行不中断提问」 |
| **② 是怎么修改的** | 能自动的写进一条龙；要人拍的先代决（D1–D16）；真机/困惑进 T1–T11 |
| **③ 有什么作用** | 开工后不被确认弹窗打断；你回来只改一份表 |
| **④ 对工作流的影响** | 不改变录/编/导。真麦、点击边角、监控拦截仍留待你检验 |

## 拍板入档（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `12` §九改为已拍板；新增 `13-Windows构建与环境复盘.md`；`00`/`README`/`AGENTS.md` 同步；使用指南注明本仓 Windows 焦点与 Electron 身份 |
| **② 是怎么修改的** | 按你确认的「按推荐执行」五条写死；复盘只写仓库里能核实的提交与命令，不编造未导出的报错 |
| **③ 有什么作用** | 决策可回看；下次装环境有最短路径；指南不误导「必须改成只写 Windows」 |
| **④ 对工作流的影响** | 不改变录/编/导。安装包窗口可能叫 Electron，已在 `02` 写明 |

## 方案完善（2026-09-06，无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新增/重写 `12-综合完善方案.md`；对齐 `00`/`10`/`11`/`README`/`AGENTS.md`；给 `02`–`08` 加过时声明 |
| **② 是怎么修改的** | 以工作区 2 场 PI（54 条）为诉求真源，消解旧稿自相矛盾，补上安装包轨道与漏评的 Windows PR |
| **③ 有什么作用** | 执行口径只剩一份；确认前不再改产品代码 |
| **④ 对工作流的影响** | 不改变录制/编辑/导出；等确认后再按 `12` §5.4 动手 |

## 变更 3 · VFR 预览时钟冻结修复（来自 PR #855）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/video-editor/videoPlayback/videoEventHandlers.ts` 与 `videoEventHandlers.test.ts` |
| **② 是怎么修改的** | 移除 `requestVideoFrameCallback`（rVFC）驱动预览时钟的逻辑（`PresentedFrameMetadata`/`PresentedFrameVideoElement` 类型、`videoFrameRequestId`、`getPresentedTime`），改为统一用 `requestAnimationFrame` + `video.currentTime` 驱动 `updateTime()` |
| **③ 有什么作用** | 修复 VFR（变帧率）素材在编辑器预览时时钟冻结/不前进的问题（#854） |
| **④ 对工作流的影响** | 仅编辑器预览时钟驱动方式变化；不改变录→停→编→导流程，无新入口、无默认行为变化 |

- 提交：`d8fe5eba` `fix: keep preview clock moving between VFR frames`

```
PR #855  标题：fix: keep preview clock moving between VFR frames
闸门：范✓ 标✓ 六✓ 体✓
R=4  C=4  E=4  S=4.00
象限：优先（R 过线）
低垂果实？风险低✓ 效果好✓ 修复简单✓
默认行为改变？否
集群：编辑器预览  角色：主方案
结论：已合（cherry-pick 干净，测试/tsc/lint 全绿）
一句话理由：编得动（G1+G2）；删 rVFC 换 rAF，体量小且无回归
```

---

## 变更 4 · 播放头秒数进位到分钟（来自 PR #671）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/components/video-editor/timeline/core/time.ts` 与 `time.test.ts` |
| **② 是怎么修改的** | `formatPlayheadTime` 由「先除 60 取分/秒」改为「先按 0.1 秒精度四舍五入（tenths），再 `Math.floor(tenths/600)` 取分、`(tenths - min*600)/10` 取秒」；补测试 |
| **③ 有什么作用** | 59.97s 不再渲染成「60.0」这类不进位的怪值，正确滚动到下一分钟 |
| **④ 对工作流的影响** | 纯时间线显示修正；不改变录/编/导任何行为 |

- 提交：`8590ca40` `fix(timeline): roll playhead seconds into the next minute [G4] (#671)`

```
PR #671  标题：fix(timeline): roll playhead seconds into the next minute
闸门：范✓ 标✓ 六✓ 体✓
R=4  C=3  E=2  S=3.20
象限：顺手
低垂果实？风险低✓ 效果好□ 修复简单✓   （效果好偏弱）
默认行为改变？否
集群：时间线显示  角色：主方案
结论：已合（cherry-pick 干净，测试/tsc/lint 全绿）
一句话理由：体验项（G4 弱）；体量极小、无回归，按 D1 顺手合
```

---

## 变更 5 · Windows 麦克风 sidecar 原子发布（来自 PR #785）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/ipc/constants.ts`、`recording/diagnostics.ts`（+测试）、`recording/prune.ts`、`register/recording.ts`、`utils.ts`、`electron-env.d.ts`、`src/.../audio/useSourceAudioFallback.ts`（8 文件） |
| **② 是怎么修改的** | 手动移植（原 cherry-pick 冲突）：FFmpeg 麦 sidecar 改写到 `.incomplete` 暂存路径→`moveFileWithOverwrite` 原子 rename 发布；新增 `isFinalizedWavFile`（检测 0xFFFFFFFF 未封装的 WAV 头）；主进程维护 `pendingCompanionAudioSidecars` 集合，`get-video-audio-fallback-paths` 返回 `pending`，编辑器据 pending 定时重查；`useScreenRecorder.ts` 未改（现树已用 `Promise.allSettled` 实现同语义） |
| **③ 有什么作用** | 修复 Windows 麦克风 15–20s 后截断：编辑器不再提前读到「看似合法却截断」的未封装 WAV（G1 听得清 + G3） |
| **④ 对工作流的影响** | 不改变录/编/导；副作用是 sidecar 发布稍晚几秒，编辑器自动重查补齐 |

- 提交：`0a5dd9f5` `fix(recording): publish Windows mic sidecar atomically [G1][G3] (#785)`

```
PR #785  标题：fix(recording): publish Windows mic sidecar atomically
闸门：范✓ 标✓ 六✓ 体✓
R=3  C=5  E=5  S=4.20
象限：优先（R 刚过线）
低垂果实？风险低□ 效果好✓ 修复简单□   （动音频链路，不低垂）
默认行为改变？否（内部实现变化）
集群：录音可靠性  角色：主方案
结论：已合（手工移植 + isFinalizedWavFile 测试，120 测试文件全绿）
一句话理由：金路径「听得清」硬修复；冲突已按当前结构逐文件重写
```

---

## 变更 6 · 编辑器返回录制 + 项目菜单（来自 PR #796）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/main.ts`、`preload.ts`、`electron-env.d.ts`、`src/lib/shortcuts.ts`、11 个 locale `editor.json`、`project/useProjectOpenActions.ts`、`useEditorProjectController.ts`、`layout/EditorHeader.tsx`、`EditorShell.tsx`、`VideoEditor.tsx` |
| **② 是怎么修改的** | 手动移植（原 cherry-pick 冲突：VideoEditor 已拆分为 EditorHeader/EditorShell + project hooks）。拆两个提交：① 基础设施（`switch-to-recording` IPC 复用 `closeEditorWindowToHud` + preload 桥接 + FIXED_SHORTCUTS 加 Ctrl+N/S/Shift+S/O + 11 locale 文案）；② UI（header 相机按钮「返回录制」+「项目」下拉菜单，错误屏加按钮，`handleReturnToRecording` 落在 useProjectOpenActions 复用未保存确认对话框） |
| **③ 有什么作用** | Windows 从此**能保存/打开项目**（之前 `Menu.setApplicationMenu(null)` 导致无入口，G1 存得了）；新增显式「返回录制」入口，不必杀进程 |
| **④ 对工作流的影响** | **新增入口、不拆旧习惯**：关编辑器窗口仍可退回录制；保存/打开多了菜单和快捷键 |

- 提交：`2c0263c0`（基础设施）+ `5a575f10`（UI）+ `aa67fb78`（手册同步）

```
PR #796  标题：feat(editor): return-to-recording action and project menu
闸门：范✓ 标✓ 六✓ 体✓
R=3  C=5  E=4  S=4.15
象限：优先
低垂果实？风险低□ 效果好✓ 修复简单□
默认行为改变？是（新增入口，默认仍可关窗口）→ 手册：已同步 R20260906-01/02+05
集群：编辑器入口  角色：主方案
结论：已合（手工移植，typecheck/lint/120 测试文件全绿）
一句话理由：金路径「存得了」硬修复；改操作已按 R14 同步手册
```

---

## 变更 7 · 使用指南同步（轨道 C，来自 #796 改操作）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `docs/01-Projects/R20260906-01-Recordly用户使用指南/02-操作教程-从启动录制到导出.md`、`05-单页速查卡.md` |
| **② 是怎么修改的** | 顶部加「变更提示」：新增「返回录制」（相机）与「项目」菜单（文件图标）入口、`Ctrl+N/S/Shift+S/O` 快捷键；§六/§7.3 增补保存/打开/返回入口表 |
| **③ 有什么作用** | 老用户能一眼看懂操作变化（R14），新用户能按图找到保存/返回入口 |
| **④ 对工作流的影响** | 文档性变更；明确「关编辑器窗口」旧习惯仍可用 |

- 提交：`aa67fb78`

---

## 变更 8 · Windows 安装包构建（轨道 B）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 无产品代码；`13-Windows构建与环境复盘.md` §五勾选 build 跑通 |
| **② 是怎么修改的** | `npm run build:win`（npmmirror 源，D8）完整跑通，退出码 0 |
| **③ 有什么作用** | 产出 `release/Electron-windows-x64.exe`（~177MB，NSIS + signtool 完成） |
| **④ 对工作流的影响** | 无；安装/监控拦截/金路径冒烟待真机（`16` A-B / `14` T3） |

- 提交：`8c34ebe9`

---

## 变更 9 · 移除所有指向 GitHub 的自动更新/下载/安装

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 删除 `electron/updater.ts`、`electron/updateChannel.ts`（+test）、`src/components/launch/UpdateToastWindow.tsx`；改 `electron/main.ts`、`windows.ts`、`preload.ts`、`electron-env.d.ts`、`src/App.tsx`、`LaunchWindow.tsx`、`popovers/MorePopover.tsx`、`SettingsPanel.tsx`、`electron-builder.json5`、`package.json`（共 14 文件，-1774 行） |
| **② 是怎么修改的** | 移除 `electron-builder.json5` 的 `publish`(GitHub) 与 `electronUpdaterCompatibility`；删除 `electron-updater` 依赖；删除 updater/updateChannel/UpdateToastWindow 三件套；main.ts 移除 `setupAutoUpdates`、全部更新 IPC、Help 菜单「检查更新」、更新 toast 窗口调用；windows.ts 移除 update toast 窗口创建/显示/隐藏/定位/恢复 HUD；preload/env 移除桥接与类型；前端移除更新入口与「实验性更新」开关 |
| **③ 有什么作用** | 应用不再从 GitHub 自动检查/下载/安装更新，不会静默下载又一份会被内部软件拦截的安装包 |
| **④ 对工作流的影响** | 无；不影响录→停→编→导；设置面板少一个「实验性更新」开关 |

- 提交：`9dad25fe` `chore(updater): 移除所有指向 GitHub 的自动更新/下载/安装 [G4]`

> 对应 portable 专题 `R20260906-04` 的步骤 B（关闭会把用户带回安装包的更新）——直接升级为彻底移除。

---

## 变更 10 · Portable（解压即跑）构建能力（步骤 A）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `package.json` 新增 `build:win:portable` 脚本 |
| **② 是怎么修改的** | `electron-builder --win zip`（复用 build:win 全流程，仅目标换 zip）；不改 `win.target`（`nsis` 仍留作备胎），不动 `app.setPath("userData")` |
| **③ 有什么作用** | 产出 `release/Electron-windows-x64.zip`（226MB，内含 `electron.exe`，解压即跑、无安装向导），作为对内主发产物 |
| **④ 对工作流的影响** | 无；新增一种分发形态，NSIS 仍可打；金路径不改 |

- 脚本：`build:win:portable`；产物 `release/Electron-windows-x64.zip`

> 对应 `R20260906-04/03` 步骤 A（P0）。步骤 C（userData 旁路）真机过了再做；步骤 D（单文件 portable）默认不做。

---

## 变更 11 · 打包后壁纸裂图（HTTP 映射 extraResources）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/rendererServer.ts`（+test）、`electron/main.ts`、`electron/ipc/project/manager.ts` `getAssetRootPath`、`src/lib/assetPath.ts`、`src/components/video-editor/SettingsPanel.tsx` 缩略图回退 |
| **② 是怎么修改的** | 打包渲染 HTTP 把 `/wallpapers` 指到 `resources/assets/wallpapers`；资产根路径多候选；`file://` 用 `URL` 解析；缩略图失败不再用裸 `/wallpapers/` 当 img src |
| **③ 有什么作用** | 安装包 / portable 进 Studio「图片」网格不再整页 404 裂图 |
| **④ 对工作流的影响** | 不改变录→停→编→导步骤；只修场景背景缩略图能否显示 |

- 专题：`R20260906-04/04-问题_Studio自动缩放与壁纸资源.md`

---

## 变更 12 · 录完拖放自动进时间轴 + 本机配 CMake 重编 helper

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 已有：`zoomSuggestionUtils.ts`（+test）、`electron/native/cursor-monitor/src/main.cpp`、`electron/ipc/cursor/monitor.ts`。本次：`scripts/ensure-windows-cmake.mjs`、`scripts/build-cursor-monitor.mjs`、`package.json`（`ensure:cmake`）、`CMakeLists.txt`（`/utf-8`）、重编 `electron/native/bin/win32-x64/cursor-monitor.exe` + `helpers-manifest.json` |
| **② 是怎么修改的** | 算法认停留启发式并把拖放区间拉到 mouseup；helper 低级钩子输出按下/抬起。CMake：PATH → 常见路径 → winget `Kitware.CMake` → 官方 zip。本机 winget 已装 CMake 4.4.3。VS 安装器未登记实例时走 `vcvars64 + NMake`。已强制编出带 INTERACTION 的 exe |
| **③ 有什么作用** | 打包后 uiohook 失效时，仍能从 helper / 停留启发式自动加缩放块，拖放在时间轴上能看见 |
| **④ 对工作流的影响** | 不改操作步骤；录完进 Studio 应自动出缩放块（偏好默认仍开）。须用新打的包验 |

---

## 变更 13 · 界面默认简中，语言列表只留中英

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/i18n/config.ts`、`src/contexts/I18nContext.tsx`、`MorePopover.tsx`、`SettingsPanel.tsx`（应用语言 + 字幕语言）、`electron/main.ts` 应用菜单、`zh-CN`/`en` 的 `editor.json`（外观）、`scripts/i18n-check.mjs` |
| **② 是怎么修改的** | 默认 locale `zh-CN`；支持列表只 `zh-CN`/`en`；localStorage 键改为 `recordly.locale.v2` 以免旧 `en` 锁死；字幕下拉改为自动检测/简体中文/English；Windows 菜单中文化。其他语种 JSON 留在磁盘不进选择器 |
| **③ 有什么作用** | 界面默认简体中文；不再出现一长串多语种列表 |
| **④ 对工作流的影响** | 不改变录/编/导。需要英文的人在设置里改。旧偏好「界面语言=英文」会被新 key 冲掉，冷启动变回简中 |

- 专题：`R20260906-05-Recordly界面语言中英收敛`（`02-问题与已落地纠正.md`）

---

## 变更 14 · Portable 压缩包命名 + 打出最新包

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `scripts/build-win-portable.mjs`、`package.json` 的 `build:win:portable`；`tsconfig.json` 排除 native `build/`；wgc-capture / gpu-export 在本机 VS 生成器失败时回退 NMake 或已暂存 exe |
| **② 是怎么修改的** | ZIP 的 artifactName 固定为 `recordly-portable-${version}.zip`（当前 `1.4.0-beta.1`）；输出目录 `release-portable/`，避免占用中的 `release/win-unpacked`。未改 NSIS 文件名 |
| **③ 有什么作用** | 分发压缩包一眼能看出 portable + 版本；本次打出含壁纸/缩放/中文界面修复的最新 ZIP |
| **④ 对工作流的影响** | 打开方式仍是解压后双击目录里的 exe；文件名从 `Electron-windows-x64.zip` 换成 `recordly-portable-1.4.0-beta.1.zip` |

- 产物：`release-portable/recordly-portable-1.4.0-beta.1.zip`（并复制到 `release/`）

---

## 变更 15 · 中英混杂 + 壁纸裂图 + 遥测漏检（再验纠正）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `MorePopover` 外观文案；`ExportSettingsMenu` 导出按钮；`zh-CN`/`en` 的 launch/settings/editor/timeline/common；`useTimelineZoomActions`；`assetPath.ts`；`telemetry.ts` persist；`get-cursor-telemetry` |
| **② 是怎么修改的** | 外观改读 `editor.theme.*`；导出补「编码/快速/均衡/画质」、按钮改为「导出视频/导出 GIF」；HTTP 窗口 `getAssetPath` 不再返回 `file://`；persist 空写不删 sidecar，读取可回退 `currentVideoPath`；时间轴提示走 i18n |
| **③ 有什么作用** | 简中界面不再出现 APPEARANCE / Presets / ENCODING / 导出Video；打包壁纸缩略图走 `/wallpapers`；录完遥测 sidecar 不易被二次 finalize 抹掉 |
| **④ 对工作流的影响** | 不改变录→停→编→导步骤；只修显示与自动缩放数据能否留下 |

- 专题：`R20260906-05/03`、`R20260906-04/05`、`R20260906-04/06`

---

## 变更 16 · 控制条 / 摄像头成片用法写入手册（无产品代码）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `R20260906-01` 的 `README`、`01`、`02`、`05`；`17`/`18` 结案；`14` T13 关闭 |
| **② 是怎么修改的** | 纠正「− = 不进成片」的误写；写清三点菜单「从录制中隐藏 HUD」、悬浮预览 vs 编辑器「显示」；标明功能已齐、不加开关 |
| **③ 有什么作用** | 你会用：录时看见条和自己，成片可以不要条、不要头像 |
| **④ 对工作流的影响** | **操作方式说明纠正**（原先手册把 − 写成不进成片）。产品行为不变。请按 `02` §三用三点菜单和编辑器「显示」，不要再靠 − 排除采集 |

- 专题：`R20260906-01`；调查结案 `R20260906-02/17`、`18`

---

## 变更 17 · 壁纸 IPC 缩略图、遥测空写、界面精简与中英弹框

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `assetPath`/`assets` 壁纸缩略图；`telemetry` 空写与 persist；`EditorHeader`/`EditorSidebar`/`App`；快捷键与项目弹框 i18n；关窗不再 `showMessageBoxSync`；版本 `1.4.0-beta.3` |
| **② 是怎么修改的** | `/wallpapers/…` 解析到 extraResources 再出 JPEG data URL；空遥测不删 sidecar、persist 不清 pending；去掉 Discord/反馈/账号；顶栏加保存；标题拼版本；关窗走同一套未保存对话框 |
| **③ 有什么作用** | 背景图能出缩略图；鼠标数据更不容易丢；界面少社区入口；弹框不再中英两套皮 |
| **④ 对工作流的影响** | 保存多一个顶栏磁盘按钮，旧菜单/`Ctrl+S` 仍可用。关窗确认从系统原生框换成编辑器内弹框，选项仍是保存/放弃/取消 |

- 专题：`R20260906-04/07` `08`、`R20260906-05/04`、`R20260906-07`

---

## 变更 18 · 硬编码英文全面接到 i18n

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `zh-CN`/`en` 的 common/editor/settings/timeline/launch；编辑器壳/设置/时间轴/导出菜单；录制 hook；项目打开保存 toast；`I18nContext` 持久化；`electron/uiLocale.ts` 与项目/字幕/导出文件对话框 |
| **② 是怎么修改的** | 用户可见英文改走 `t()`；主进程对话框读 `app-settings.uiLocale`；失败 toast 用已译词条，不再把英文 `result.message` 弹给用户 |
| **③ 有什么作用** | 默认简中时界面、toast、alert、系统选文件标题一致，不再中英夹杂 |
| **④ 对工作流的影响** | 不改录→停→编→导步骤，只改文案语言 |

- 专题：`R20260906-05/05`

---

## 变更 19 · Whisper 模型随包 + 升版 Portable

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/ipc/captions/whisper.ts`、`generate.ts`、字幕 IPC/预加载类型；`SettingsPanel` / `useAutoCaptionController`；`electron-builder.json5` extraResources；`scripts/ensure-whisper-model.mjs`、`build:win`/`build:win:portable`；`smoke-packaged-binaries.mjs`；版本 `1.4.0-beta.4`；手册 `R20260906-01/04` |
| **② 是怎么修改的** | 构建前校验 `.tmp/whisper-models/ggml-small.bin`，打进 `resources/whisper/`。启动状态优先 userData，否则捆绑。删除只删用户副本。界面有捆绑时显示「已内置识别模型」 |
| **③ 有什么作用** | 培训师打开即可「生成字幕」，不必再从 Hugging Face 下 466MB 模型 |
| **④ 对工作流的影响** | 金路径多了「能直接出字幕」。操作上少一步下载。Portable zip 大约增加 466MB。不改变录→停→编→导顺序 |

- 产物：`release-portable/recordly-portable-1.4.0-beta.4.zip`
- 专题：`R20260906-08`

---

## 变更 20 · 字幕面板去掉下载/选择模型，动画改中文

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `SettingsPanel.tsx` 字幕区；`useEditorSettingsPanelProps` / `VideoEditor` 不再往面板塞模型选择 props；`whisper.ts` 增加 exe 旁 `resources/whisper` 候选；`R20260906-08/06`、手册 `04` |
| **② 是怎么修改的** | 面板只留语言 + 清除/生成；生成不再因 `whisperModelPath` 为空而禁用。动画选项改读 i18n（关闭/淡入淡出/上移/弹出） |
| **③ 有什么作用** | 培训师不再被下载、手选 `.bin` 挡住；生成按钮可点 |
| **④ 对工作流的影响** | **操作方式变了**：不要再找「下载模型」「选择模型」。打开字幕面板 → 选语言 → 生成。旧 zip 界面仍是老按钮，需用含本改动的构建 |

- 专题：`R20260906-08/06`

---

## 变更 21 · Portable 字幕认 exe 旁模型 + 默认简中

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/ipc/captions/whisper.ts`、`generate.ts`、`whisper.test.ts`；字幕默认语言 `zh`；面板去掉语言下拉；手册 `04`、`R20260906-08/07` |
| **② 是怎么修改的** | 查找不依赖 `isPackaged`；优先 `exe/resources/whisper/ggml-small.bin`；半截 userData 文件不算；生成固定 `-l zh` |
| **③ 有什么作用** | 完整解压 Portable 后生成字幕应能找到随包模型；讲解默认按简体中文识别 |
| **④ 对工作流的影响** | 操作：完整解压 → 开 exe → 字幕面板直接生成。不再选语言。从 zip 窗口直接跑仍可能失败 |

- 专题：`R20260906-08/07`

---

## 变更 21 · 打包后背景图网格走 IPC data URL

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/ipc/assets/wallpaperPath.ts`；`assets` IPC / preload；`assetPath.ts`；`SettingsPanel` 背景网格；`rendererServer` extraRoots；`getAssetRootPath`；`R20260906-04/09`；版本 `1.4.0-beta.5` |
| **② 是怎么修改的** | 缩略图/整图主进程返回 data URL 字符串。打包态网格、默认预览画布、视频格都禁止把 `/wallpapers/…` 当 src。HTTP extraRoots 文件不在则回退 dist；builder 不再排除 `dist/wallpapers`。视频走磁盘 + 媒体服务 |
| **③ 有什么作用** | Portable 里 Studio → 背景 → 图片能出缩略图，不再 24 格裂图。开发态仍走 Vite，体验不变 |
| **④ 对工作流的影响** | 不改录→停→编→导。旧 zip 仍裂，需用含本改动的 Portable |

- 版本：`1.4.0-beta.5`
- 专题：`R20260906-04/09`

---

## 变更 22 · 按次启动会话日志与金路径埋点

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/sessionLog*.ts`；`main` / IPC / preload；录制、选源、光标、字幕、导出；`notifyError`；启动窗打开日志文件夹 |
| **② 是怎么修改的** | 每次启动一份 JSONL；挂钩 console 与崩溃；金路径一等事件；用户可见错误先入档 |
| **③ 有什么作用** | 能从这一次启动的文件定位录/停/编/导失败与静默回退 |
| **④ 对工作流的影响** | 不改金路径步骤。更多菜单多「打开日志文件夹」（可选） |

- 专题：`R20260906-11`

---

## 变更 23 · 版本 1.4.0-beta.6 与 Portable 打包

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `package.json` / `package-lock.json` 版本；`EditorPreviewPanel` / `EditorVideoPreview` 的 `setError` 类型 |
| **② 是怎么修改的** | 版本 `1.4.0-beta.5` → `1.4.0-beta.6`。预览 `setError` 改为只接 `string \| null`，与 `useProjectState` 包装后的 setter 对齐，解除 `tsc` 阻断 |
| **③ 有什么作用** | 能打出含会话日志、采集档、字幕模型等当前改动的 Portable zip |
| **④ 对工作流的影响** | 不改录→停→编→导。解压后双击 `Electron.exe`，文件名为 `recordly-portable-1.4.0-beta.6.zip`。**事后对照**：此 zip 第一次带上 `9ba9a0f3` 的 preview-yield，Beta 4/5 能播、Beta 6 闪屏即此包 |

- 版本：`1.4.0-beta.6`
- 产物：`release-portable/recordly-portable-1.4.0-beta.6.zip`
- 回归说明：`R20260906-12/10` §2 版本对账

---

## 变更 24 · 编辑器播放闪屏：mux/录制不再停预览

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `shouldYieldPreviewForPhase`；`sessionLog.ts`；`recording.ts` 停录/mux `finally`；`EditorShell` 注释 |
| **② 是怎么修改的** | P2 让路从 recording/mux/export/captions 收到只剩 export/captions；mux 失败也切回 editor |
| **③ 有什么作用** | 停录进 Studio 能播、不闪；导出/字幕仍让路 |
| **④ 对工作流的影响** | 不改录→停→编→导步骤。刚停录即可回放 |

- 专题：`R20260906-12/10`
- 回退点：`00781b78`

---

## 变更 25 · 版本 1.4.0-beta.7（闪屏让路回收入档）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 版本号；`12` 夹 `10`/`11`/`12` 调查；让路代码（见变更 24） |
| **② 是怎么修改的** | `1.4.0-beta.6` → `1.4.0-beta.7`。与回收 recording/mux 让路同一提交 |
| **③ 有什么作用** | 下一包与已确认回放坏掉的 Beta 6 分开；文档可回退 |
| **④ 对工作流的影响** | 不改步骤。请用新打的 beta.7 zip 验 T14，不要继续用 Beta 6 目录 |

- 版本：`1.4.0-beta.7`
- 产物：`release-portable/recordly-portable-1.4.0-beta.7.zip`（约 685MB，2026-09-06 19:22）
- 用法：完整解压后双击目录里的 `electron.exe`，不要用旧的 Beta 6 目录

---

## 变更 26 · 打出 1.4.0-beta.7 Portable ZIP

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 构建产出 `release-portable/recordly-portable-1.4.0-beta.7.zip`；本机构建顺带重编并暂存 `wgc-capture` / `cursor-monitor` / `gpu-export` helper（未另开功能改动） |
| **② 是怎么修改的** | `npm run build:win:portable`（npmmirror、已有 ggml-small.bin） |
| **③ 有什么作用** | 真机可用含让路回收的包验进编辑器播放 |
| **④ 对工作流的影响** | 不改步骤。旧 Beta 6 zip/目录不会自己变好 |

---

## 变更 27 · 撤回 preview-yield，并修编辑器播放 pause 环

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `sessionLog.ts` / `sessionLogFormat.ts`；`EditorShell.tsx`；`VideoPlayback.tsx`；`recording.ts` 注释；`sessionLogFormat.test.ts`；`R20260906-12/10` §8–§9 |
| **② 是怎么修改的** | 整条 `preview-yield` 撤掉（不 revert 72dd1731，避免 mux 再停预览）。纹理/镜头 effect 不再绑 `layoutVideoContent` 身份；恢复播放先开 `allowPlaybackRef`；已在播时 `loadedmetadata` 不再 pause |
| **③ 有什么作用** | beta.7 仍闪、没声、进度钉死。对准播放被反复打断，而不是再收让路相位 |
| **④ 对工作流的影响** | 不改录→停→编→导。导出时预览仍停。无新入口 |

---

## 变更 28 · 第 1 包 1.4.0-beta.8 供逐步验收

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 版本号；`R20260906-12/14` 验收卡；产品改动见变更 27 |
| **② 是怎么修改的** | 只打编辑器对齐包，采集默认不动 |
| **③ 有什么作用** | 与 beta.7 区分；你验 1/2/3 后我们开下一刀 |
| **④ 对工作流的影响** | 不改步骤。解压新目录，不要用 Beta 6 / beta.7 文件夹 |

---

## 变更 29 · 回放金路径会话日志

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `previewPlaybackLog.ts`；播放控件 / VideoPlayback / videoEventHandlers；开录带采集档 |
| **② 是怎么修改的** | `preview.*` 事件入本次 JSONL；限流避免 pause 环刷盘 |
| **③ 有什么作用** | 第 1 包验收能靠日志判 H1/H3，不靠猜 |
| **④ 对工作流的影响** | 不改步骤。验完打开日志文件夹，搜 `preview.` / `capture.start` |

---

## 变更 30 · beta.8 日志：稳住 setError + helper 走 unpacked

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `useProjectState.setError`；Pixi 挂载一次；`resolveUnpackedAppPath` |
| **② 是怎么修改的** | 对账 `recordly-20260906-200412-pid12656.log` 后改主开关，不撤让路/pause 环 |
| **③ 有什么作用** | 回放不再拆画布；Portable 本机采集不再 ENOENT |
| **④ 对工作流的影响** | 不改步骤。请用 beta.9 新目录验 |

---

## 变更 31 · 摄像头中英文案 + 全站优雅图贴 + 导出 ffmpeg 路径（beta.10）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `settings.json` / `timeline.json` 中英；`AppTooltipHost` + `Button`/`ToggleGroupItem`；侧栏/时间轴/启动窗剩余 `title`；`ffmpeg/binary.ts`（上一专题已改，本包带上）；版本 `1.4.0-beta.10` |
| **② 是怎么修改的** | 缺 key 补齐，不再掉英文 fallback。`title` 改 `data-tooltip`，暗色圆角浮层。FFmpeg 看见 asar 就改写 unpacked |
| **③ 有什么作用** | 摄像头面板简中不再夹英文；图贴不再是系统黑框；Lightning 导出不再 spawn 进 asar |
| **④ 对工作流的影响** | 不改录→停→编→导。解压 **新** `recordly-portable-1.4.0-beta.10` 目录再验 |

- 专题：`R20260906-05/06`、`R20260906-13`
- 产物：`D:\code\Recordly\release-portable\recordly-portable-1.4.0-beta.10.zip`

