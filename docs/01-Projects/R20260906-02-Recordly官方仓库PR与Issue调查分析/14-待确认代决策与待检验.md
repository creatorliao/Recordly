# 14 · 待确认（代决策）与待检验

> 执行中**不中途问你**。需要你拍板的，我先按角色/G/平衡矩阵**代做决定**，写在本文 §二，你以后逐条改「同意 / 改判」即可。  
> 特别困惑、自动化覆盖不了、或合完必须人眼看的，写在 §三，你回来再检验。  
> 与 [`12` §十](12-综合完善方案.md)、[`15-执行中暂缓与跳过.md`](15-执行中暂缓与跳过.md) 配套。  
> **合不进去的 PR 不要记在本文**，记 `15`。本文只留：事先代决、合上后要你真机看的。

**状态约定**

| 标记 | 含义 |
|------|------|
| 代决 · 待你勾 | 已按推荐拍板，默认按此执行；你可事后改 |
| 待检验 | 自动化做不到或风险含糊，不阻塞队列 |
| 已关闭 | 你勾过或执行中已消掉 |

你回来时只动这一份：在「你的回复」列写同意/改判即可，不必再开新聊天解释背景。

---

## 一、和执行自动化怎么配合

- 开工后按 `12` §5.4 一条龙做：取 PR → 填平衡卡 → cherry-pick → `npm test` / lint / typecheck → 留痕 → 下一条。
- **卡住不举手**：综合考量过不去、且按 D2/D3/D6 已经能决定「暂缓」→ **先写 [`15`](15-执行中暂缓与跳过.md) 一条完整记录**，再做下一条。不要只在聊天里说一声。金路径失败则 revert 后再记 `15`。连续两条金路径 revert 才整轨暂停（D11）。
- 只有「开始做轨道 A / B」需要你开口一次；过程中的选择题全部落在 §二。

---

## 二、代决策清单（请事后逐条确认）

| # | 议题 | 我代做的决定 | 依据 | 你的回复 |
|---|------|--------------|------|----------|
| D1 | 可选 #655、#671 做不做 | **做**，紧接 #855 之后；任一条失败则跳过，不阻塞 #803 | 过闸、体量极小、象限「顺手」；不插到金路径前面 | |
| D2 | cherry-pick / rebase 冲突 | 自动 rebase **一次**；仍冲突 → 不手搓；**记 `15` 暂缓**，做下一条 | 决策过得去：就是暂缓 | |
| D3 | #803 测试失败或像误穿透 | **revert**，不现场调参；**记 `15`**；点边角真机仍留 T4 | 刚过 500 行，乱改不如退 | |
| D4 | #785 无法在本机自动「讲 3 分钟」 | AI 只跑自动化测试；3 分钟麦测标 **T1 待检验**，不因此停队列 | 降低你现场工作量 | |
| D5 | #796 改操作 | AI **直接改**使用指南 `02` + 速查卡「变更提示」；你事后看手册（T2） | 已拍板必更手册；不把「手册好不好懂」挡在合并前 | |
| D6 | #855 diff 与当前树对不上 | **跳过**，**记 `15`**；继续 #655 | 上游 main 可能已修 | |
| D7 | #620 / #688 | **不另合**，做 #803 时对照是否已覆盖；没覆盖只记 §三 | 避免叠修 | |
| D8 | npm 源 | 本机构建/装依赖优先 **国内镜像**（npmmirror） | 你的环境约定 | |
| D9 | 是否 push 远程 | **不 push**，直到你以后明确说推 | 本地 `dev-creator` | |
| D10 | 开工后还要不要再问队列 | **不问**；以 `12` §5.4 + 本文 D1 为准 | 已拍板 | |
| D11 | 连续两条金路径（#803/#785/#796）都 revert | **暂停轨道 A**，在 §三置顶「请先看」 | 金路径连续失败再问你才值 | |
| D12 | 轨道 B `build:win` | 「开始做 B」后 AI 自动打包装；拦不拦截、装不装得上标 **T3** | AI 不能代替你们监控软件 | |
| D13 | 平衡卡 | 每合一条，卡填进 [`09`](09-执行留痕.md) 该条下面 | 附录 A 强制 | |
| D14 | 提交信息 | `fix\|feat(scope): 摘要 [G?] (#号)`，squash 成一个本地 commit | 便于 revert | |
| D15 | #421/#422/#659/#634 | 本轮仍不做；只留 §三备忘 | 已拍板暂缓 | |
| D16 | 手册仍写 macOS/Linux | **保持**，并保留「本仓焦点 Windows」那句 | 已拍板 | |

---

## 三、困惑 / 有问题 · 待你回来检验

自动化或代决策盖不住的，放这里。**不阻塞开工。**

| # | 严重度 | 题目 | 为什么困惑 | 建议你怎么检验 | 状态 |
|---|--------|------|------------|----------------|------|
| T1 | 高 | #785 讲解声是否真的不再 15–20s 断 | 必须真麦、真录一段 | 录 3 分钟讲话，听全程；导出再听一遍 | 待检验 |
| T2 | 中 | #796「返回录制 / 保存项目」好不好找、会不会让老人迷路 | 交互题，测试绿了也不等于不困惑 | 按旧习惯关编辑器一次，再找新入口；看手册变更提示是否一读就懂 | 待检验 |
| T3 | 高 | 内部监控是否仍拦截安装包 | 只有你这台机器上的监控说了算 | 装 `release/Electron-windows-*.exe`，看是否被拦、进程是否像 Electron | 待检验 |
| T4 | 高 | #803 会不会点穿不该点的 HUD 按钮 | 点击穿透经典误伤；单测难覆盖「刚好点到边」 | 录制中：点被录软件底部按钮（必须中）；再点 HUD 上可见控件（必须还能点） | 待检验 |
| T5 | 中 | #855 对你有没有意义 | 你未必用 VFR 源 | 若从不编变帧率录像，可事后判「白合」，revert 成本低 | 待检验 |
| T6 | 中 | 当前 `dev-creator` 相对上游是否已含部分修复 | 维护者合自己 PR 极快，社区 PR 易过期 | 开工时 AI 会先 `git log` / 搜关键符号；你若发现「这不是早就有了吗」在此补一行 | 待检验 |
| T7 | 中 | 包装名叫 Electron，学员/同事会不会问「这是什么软件」 | 身份改动是你要求的，和教学品牌打架 | 自己用可以；若要对外发教程再决定要不要加窗口副标题（另议，本轮不做） | 待检验 |
| T8 | 低 | 真光标与渲染光标重叠（#634/#659） | 无干净小 PR，Windows 上可能仍丑 | 录一个窗口，看是否双箭头；有则拍照附到本表，下轮再找 PR | 待检验 |
| T9 | 中 | rebase 后的 #803/#785 还是不是原来那份修复 | DIRTY PR 解完冲突可能改味 | 对照原 PR 的测试名/关键注释是否还在 | 待检验 |
| T10 | 低 | 无头环境跑不了真录屏冒烟 | AI 只能保证单测+静态检查 | 你最后的手工走查仍按金路径 6 步；这是故意留给你的 | 待检验 |
| T11 | — | （执行时追加） | cherry-pick 失败、测试红、行为怪异 | 开工后 AI 往下追加行，不另开文档 | 预留 |

**置顶规则**：若触发 D11（连续两条金路径 revert），在本表最上方加一行：

```
【请先看】轨道 A 已暂停。revert 的 PR：#… #…  日志：……
```

---

## 四、你回来时的最短用法

1. 先扫 §二「你的回复」空着的行，写「同意」或「改成：……」。
2. **走查和打勾按 [`16-人工验收清单.md`](16-人工验收清单.md)**（T1→A-785，T3→A-B，T4→A-803，T10→A-G）。本文 §三只保留「为什么要人看」，不再当操作步骤。
3. 改判若影响队列，回写 `12` §九。
