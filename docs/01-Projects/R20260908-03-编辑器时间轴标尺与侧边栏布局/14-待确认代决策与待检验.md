# 待确认代决策与待检验

## 一、进度

文档止。未改产品代码。等人在对话里确认方案（或改选项）并说「开始做」。

## 二、代决策（未拍板前按推荐执行）

| # | 题 | 推荐 | 备选 |
|---|----|------|------|
| D1 | 「太宽」修水平走廊 + 竖直标尺 | 都修（S1-A） | 只改通栏、不动标尺皮肤 |
| D2 | 运输条与标尺缝 | **4px** | 8px |
| D3 | 底栏 vs 左轨 | **B：左轨通到窗口底，底栏只在内容列** | A：底栏全宽 + 文字避开 48px |
| D4 | 样式预设 | **只挂场景**；光标页一行链回场景；裁剪/frame 不挂 | 维持 D8 双挂 + 说明文案 |
| D5 | 播放键位置 | **不搬**，仍在预览列底 | 搬进时间轴顶（本夹不采用） |
| D6 | 时间轴右沟 | **废止**（PR-06）：去掉 `pr-3`，不另留沟 | 保留 4px（不推荐） |
| D7 | 块内图标与两行字 | **去图标 + 单行居中 11px**；时码/Auto 进 tooltip | 留第二行 |
| D8 | 齿轮对齐 | 轨根 `items-center` | 只改齿轮尺寸 |
| D9 | 抽屉皮 | 认 `R20260907-02/03` 一次收齐控件 | 只改光标页 |
| D10 | 抽屉底垫 | **12px** | 16px |
| D11 | 音量 | 扁轨 + 外侧 %，位置仍运输条右 | 系统原生 range |
| D12 | 折叠钮 | 纯 Caret，无方块无阴影 | ghost 方钮 |

## 三、须真机看（合上后）

1. 最大化 / 还原窗口：左轨不断开，齿轮不进时间轴。
2. 抽屉开/关：时间轴左缘始终贴抽屉右缘（关抽屉则贴 48px 轨）。
3. 拖高时间轴、双击抓手、折叠：预览反撑，抓手不压播放。
4. 播头拖到 0 与片尾：手柄不挡「添加图层」和音量。
5. 场景保存预设 → 切光标：列表不出现第二张卡；链能回到场景。
6. 浅色/深色各看一次边线。
7. 最大化：内容列右缘贴窗，**不应**再有 12px 白边。
8. 时间轴块内无放大镜/气泡/鼠标图标；字幕垂直居中。
9. 左下齿轮与左上图标同一垂直中线。
10. 抽屉滚到底，最后一排按钮离底 ≥12px。
11. 音量是扁轨，百分比在轨外。
12. 折叠钮无底无边。
