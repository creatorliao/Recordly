# 04 · 实施计划（P15）

> 方案：[`03`](03-方案_通用客户端与Recordly落地.md)。等「开始做」，不改代码。  
> 与 `R20260906-14`：工具栏窗 / 倒计时 / 追加录以 14 为准；本夹只加进门。卡 `main.ts` 时跟 14 的 P02-B2 错开或同人顺做。

## 目标

设置打开后，资源管理器右键出现 Recordly 级联：开始录制、打开录制工具栏、用 Recordly 打开。点了走单实例 + CLI 动词。关掉或卸载后菜单消失。只写 HKCU。

## 路径

| WP | 做 | 文件（确认后） |
|----|----|----------------|
| S0 | 设置项 + HKCU 写/删级联三键（文件 / 目录 / 空白处）；语言切换重写显示名 | 新模块如 `electron/windowsExplorerContextMenu.ts`、设置 store、设置面板一行 |
| S1 | `parseLaunchRequest`；`whenReady` / `second-instance` 分发 `--open` / `--hud` / `--record` | `electron/main.ts`、已有打开项目 IPC、已有开始录制入口 |
| S2 | NSIS `customUnInstall` 删三棵键（安装器不写菜单，见 D15-5） | `build/installer-context-menu.nsh`、`electron-builder.json5` |
| S3 | 手册一句：右键三条分别干什么、开录用的是上次源 | `R20260906-01` 使用指南 |

一行超 500 则停在 WP 边界，不把 S0+S1 糊成一个 PR。

不抄 Win11 DLL。不接盘符。

## 验收

| ID | 标准 |
|----|------|
| V1 | 设置开：三处根键 `...\shell\Recordly` 存在，级联三项文案对 |
| V2 | 设置关：键消失，右键没有 Recordly |
| V3 | 「用 Recordly 打开」`.recordly` 进该项目 |
| V4 | 「打开录制工具栏」出条且**不开录** |
| V5 | 「开始录制」出条并进入录制（或倒计时）；已在录则只聚焦 |
| V6 | 已开实例再右键走 `second-instance`，不双进程 |
| V7 | Portable 只写 HKCU；卸装无空项 |

In：设置 + 资源管理器。Out：14 的两间房（不改条上按钮含义）。
