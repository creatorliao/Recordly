# 14 · 待确认代决策与待检验

> 推荐已写入 `04` / `05`。等人一句：**开始做**（可指定只做 WP-A / 做到 WP-C / 含 WP-D）。  
> 要推翻某条，点名编号即可。

---

## 二、代决（不写就按推荐）

| 编号 | 推荐 | 含义 |
|------|------|------|
| **D-A** | 删当前选中，**包括 clip** | 覆盖 `R20260906-14` L6「别误删整段」。有撤销。设置面板删除按钮行为不变 |
| **D-B** | 只放宽 **Delete** 的 focus 门闩 | C/Z/A/F 仍要时间线聚焦，降低预览里误分割 |
| **D-C** | 合缝为**唯一默认**，不做「自由/磁吸」开关 | 不过度。将来要留洞再开题 |
| **D-I** | **删正片区间**才全轨左移；只删字/缩放不 ripple 正片 | 对齐剪映；避免删箭头整课跳走。画面+字幕一起裁只发生在「成片时间被拿掉」时 |
| **D-D** | 换轨只在**同类轨**；默认多 1 条空占位 | 不一次铺很多空行 |
| **D-E** | 第一批 **WP-A → E → F → G → C → B → D** | D 可单独后做；「先不要动模型」则停在 A+E+F+G+B+C |
| **D-J** | 默认 **1** 条讲解声波形；缺省开；设置可关 | 不默认系统+麦两条。先本仓轨，peaks 挂了再抄 OS |
| **D-H** | 配色认 `R20260907-02` 洗色表，不另起盘 | 浅色也用 /16，禁止糖果满填 |
| **D-G** | 端点只改时长；**禁止**整块拖出轴尾撑长轴 | 叠层右缘可撑长轴；正片右缘不超过源片长 |
| **D-F** | 本夹不改版本号、不打 portable | 除非你另说发版 |

冲突记录：D-A 与旧 L6 互斥。按本次点名 + `AGENTS.md`「Delete 删当前选中」取 D-A。

---

## 三、确认后才真机看

| # | 看什么 | 为何人看 |
|---|--------|----------|
| T1 | 点预览再点时间线块，不回点空白，按 Delete | 焦点门闩只有真机手感准 |
| T2 | 选中正片 Delete；Ctrl+Z 回来 | 怕误删 |
| T3 | 输入字幕/文件名时 Delete | 不能变成删块 |
| T4 | 一条注释上下拖到新空轨 | dnd 命中与否 |
| T5 | 拉片头左缘：轴缩短，字/缩放/字幕都跟上 | WP-D |
| T6 | **C 切两刀，Delete 中间正片**：后段靠上，全轨左移，中间无洞 | 用户点名的中间裁 |
| T13 | 中间那段上有一句完整字幕：Delete 后字幕没了 | 画面字幕一起裁 |
| T14 | 一句字幕骑在中间段上：重叠截掉，两侧跟画面靠拢 | 跨段 |
| T15 | 只删一条注释：正片长度不变 | D-I / R11 |
| T7 | Ctrl+滚轮缩放，指针附近的刻度是否稳住 | 触控板/鼠标差异 |
| T8 | 导出一小节：裁过的片头不应再出现 | 合缝后的金路径 |
| T9 | 抓缩放/字幕/正片**右端点**往右拉：时长变长、块左缘不动 | 点名 4；与整块平移对比 |
| T10 | 抓**中间**平移：时长不变 | 反例对照 |
| T11 | 很短的一块：两端仍能单独拉，中间仍能搬 | 窄块外置 |
| T12 | 浅色 + 深色：块是否低饱和、选中无光晕、正片不像糖果 | 配色只有人眼准 |
| T16 | 新录 + 旧课：是否正好 1 条波形、静音/有声能分清、点波形画面跟上 | 峰值与文件因机器而异 |

---

## 四、我不会在「开始做」之前做的

- 不改产品代码
- 不改使用手册（WP-A/C/D 会改操作手感，实施时再补教程提示）
- 不提交 git
