# 09 · 执行留痕

## 2026-09-08 · 建夹与调查文档（未改产品代码）

| 要素 | 内容 |
|------|------|
| ① 改了哪些 | 新建 `docs/01-Projects/R20260908-05-Recordly时间线剪辑交互对标改善/`：`00` `01` `02` `03` `04` `05` `14` `09` |
| ② 怎么改的 | `para-structure cp` 建夹；通读 Recordly 时间线与 `D:\code\openscreen` 对照后落盘 |
| ③ 作用 | 固定需求、现状、三痛点根因、对标差距、按低垂优先的改善清单与未实施的方案，等人确认 |
| ④ 对工作流 | **无**。不改录 → 停 → 编 → 导；不改产品代码 |

调查结论摘要：Delete 为接线 + 焦点门闩（可先做）；换轨缺空轨（中低垂，OS 无现成代码）；合缝受 `startMs` 双职责限制，需适配 OS `resequenceClips` 思想后再做。

## 2026-09-08 · 补点名：边柄拉长 vs 整块平移

| 要素 | 内容 |
|------|------|
| ① 改了哪些 | `00` `01`（R6/R7）`02` `03` §4 `04` L8/WP-E `05` WP-E `14` D-G/T9–T11 |
| ② 怎么改的 | 核对 `Item.tsx` listeners、`.zoomEndCap` 4px、`resolveDragEnd` 锁时长、`getClipDragTotalMs` 整块撑轴、`resolveResizeEnd` 夹 `totalMs`；对照 OS `pillAffordance` |
| ③ 作用 | 把「两端拉时长、中间搬家」写入需求与低垂方案，实施序紧接 Delete |
| ④ 对工作流 | 仍不改产品代码 |

## 2026-09-08 · 补点名：时间轴配色收束

| 要素 | 内容 |
|------|------|
| ① 改了哪些 | `00` `01` R8 `02` `03` §5 `04` L9/WP-F `05` WP-F `14` D-H/T12 |
| ② 怎么改的 | 对照 `ItemGlass` 硬编码糖色 vs `R20260907-02` 洗色表 vs 剪映/Premiere/OS |
| ③ 作用 | 配色只落地已拍板规范，列入低垂、不另起第三套色 |
| ④ 对工作流 | 仍不改产品代码 |

## 2026-09-08 · 补点名：中间裁一段 + 全轨磁吸 + 字幕跟裁

| 要素 | 内容 |
|------|------|
| ① 改了哪些 | `00` `01` 点名1 与 R3/R9–R11 `02` `03` §1 `04` WP-D `05` WP-D `14` D-I/T6/T13–T15 |
| ② 怎么改的 | 写清剪映：删成片中间区间后全轨靠拢；字幕与画面同一区间裁切；只删叠层不 ripple 正片。核对现状不处理 `autoCaptions` |
| ③ 作用 | WP-D 不再只写「左裁」，中间裁与字幕成为硬验收 |
| ④ 对工作流 | 仍不改产品代码 |

## 2026-09-08 · 补点名：默认讲解声波形轨

| 要素 | 内容 |
|------|------|
| ① 改了哪些 | `00` `01` 点名6/R12 `02` `03` §6 `04` L10/WP-G `05` WP-G `14` D-J/T16 |
| ② 怎么改的 | OS 有块内 ClipWaveform；本仓有独立源音频轨但旧课/无峰时常 0 行。方案：先修可见性，解码不稳再抄 OS peaks |
| ③ 作用 | 用波形找有声/静音并对画面；不默认多条轨 |
| ④ 对工作流 | 仍不改产品代码 |
