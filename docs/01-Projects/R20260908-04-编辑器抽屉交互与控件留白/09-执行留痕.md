# 执行留痕

## 2026-09-08 · 去掉光标页「Settings > Dev」英文提示

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `SettingsPanel.tsx`（光标区）；`07-调查_光标页弹簧Dev提示.md`；`00`/`09` |
| ② | 是怎么修改的 | 删掉光标摆动下方仅 DEV 显示的 `cursorDebugMovedToDev` 提示框。弹簧刚度等仍只在齿轮 Settings → Dev |
| ③ | 有什么作用 | 消除「像缺了一截英文功能」的误解；光标页只留培训师用的摆动等控件 |
| ④ | 对原工作流程的影响 | 录/编/导不变。开发者仍可从齿轮 Dev 调弹簧；正式包本就无此提示 |

## 2026-09-08 · 抽屉收起后左轨熄灭高亮

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `EditorSidebar.tsx` |
| ② | 是怎么修改的 | 图标高亮改为 `settingsPanelVisible && activeSection === id`。抽屉关时蓝条/填色/不透明度全部熄灭；`activeSection` 仍保留，再开还在原页 |
| ③ | 有什么作用 | 收起后不会出现「拼图还在亮、板却没了」的假选中 |
| ④ | 对原工作流程的影响 | 点左轨开/再点关逻辑不变；顶栏抽屉钮开合不变 |

## 2026-09-08 · 导出主按钮 + 通窗底栏

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `EditorExportMenu.tsx`；`EditorShell.tsx`；`04`/`14`/`09` |
| ② | 是怎么修改的 | 导出：幽灵文字链 → 品牌蓝 `#2563EB` 实心主按钮（高 24、白字、圆角 5）。底栏：从内容列底挪到主工作区下通窗一行，左轨坐在底栏上，仿 VS Code；不用窗口外圈留白 |
| ③ | 有什么作用 | 导出在顶栏一眼能找到；窗口底有明确收口，齿轮轨不再直接顶任务栏 |
| ④ | 对原工作流程的影响 | 导出仍先开下拉再选路；保存态/时刻含义不变。D3 后说修订为通窗底栏 |

## 2026-09-08 · 图贴统一、片尾刻度、顶栏收抽屉

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `tooltip.ts` / `AppTooltipHost.tsx` / `tooltip.test.ts`；`TimelineAxis.tsx`；`EditorTimelinePanel.tsx`；`EditorHeader.tsx` / `EditorShell.tsx`；`en`/`zh-CN` `editor.json` |
| ② | 是怎么修改的 | 图贴：悬停时把原生 `title` 收成 `data-tooltip` 并摘掉 `title`，折叠钮直接走 `data-tooltip`。标尺：轴不再左缩 8px 两次；片尾数字钉在右内边，靠近片尾的中间刻度不画。顶栏：在「文件」左侧加抽屉开关（开/关 `settingsPanelVisible`），不放回导出旁 |
| ③ | 有什么作用 | 「展开时间轴」不再是系统黑框白底；`0:30` 后不再露出半个字；整抽屉可从顶栏收起/展开，左轨再点当前图标仍可关 |
| ④ | 对原工作流程的影响 | 录/编/导不变。D8「导出旁不要侧栏钮」仍成立；新钮在文件菜单旁，对齐 OpenScreen 左上 |

## 2026-09-08 · 标尺左上 0:00 让开左边线

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `src/components/video-editor/timeline/components/axis/TimelineAxis.tsx` |
| ② | 是怎么修改的 | 首刻度左对齐、尾刻度右对齐，中间仍居中。不再对 `0:00` 做 `translateX(-50%)` |
| ③ | 有什么作用 | 左上角 `0:00` 不再压进左边竖线；片尾数字也不再一半伸出右缘 |
| ④ | 对原工作流程的影响 | 只改标签对齐，seek / 轨位置不变 |

## 2026-09-08 · PR-04 折叠钮让开片尾时刻

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `EditorTimelinePanel.tsx`、`timelineLayout.ts`、`timelineLayout.test.ts`；`02`/`09`、附图-07 |
| ② | 是怎么修改的 | 去掉标尺上的 `absolute right-1 top-0`。时间轴右侧加 24px 槽，折叠钮与标尺行同高（28px），中间 1px 分割线 |
| ③ | 有什么作用 | `0:30` 等片尾刻度与箭头同时可读、可点，不再叠字 |
| ④ | 对原工作流程的影响 | 收起/展开仍是同一颗钮，只是位置改到轨右缘槽内。不改切割/缩放/导出 |

## 2026-09-08 · 立项文档止（未改产品代码）

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | 新建 `docs/01-Projects/R20260908-04-编辑器抽屉交互与控件留白/`：`00`～`04` `14` `09` 附图-01～05 `.gitkeep` |
| ② | 是怎么修改的 | PM-50 文档止。只读 `EditorShell`（footer 第三列）、`EditorSidebar`（关抽屉不重开）、`SliderControl`（72/44+absolute 轨）、`select.tsx`（Item `text-sm` + `shadow-md`）、`EditorHeader`（右上 SidebarSimple）、折叠钮叠标尺 |
| ③ | 有什么作用 | 把复验 7 条落到新主题夹；空列根因写成底栏错位，避免再挖窗口右沟 |
| ④ | 对原工作流程的影响 | **无**。未改录制/编辑/导出代码 |

下一步：决策已锁定。等人说「开始做」后按 `04` 开 WP-A → WP-C，WP-B 可并行。

## 2026-09-08 · 按推荐锁定决策并完善方案（未改产品代码）

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `04` 写成唯一规格（结构/像素/文件/WP）；`14` D1–D8 标已锁定；`02` 待确认改为决策；`00` 进度一行 |
| ② | 是怎么修改的 | 用户要求按推荐记录：底栏钉在内容列底、8px 画布内边、左轨再点关闭、删顶栏侧栏钮；补 PM-54 论证 |
| ③ | 有什么作用 | 施工时不再并列两种底栏/两种开抽屉 |
| ④ | 对原工作流程的影响 | **无**。仍未改产品代码 |

## 2026-09-08 · PR-03 复验并修好右侧空列

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | 文档：`06-调查_右侧空列与状态栏错位.md`、附图-06、`00`/`02`/`03`/`14`。代码：`src/components/video-editor/layout/EditorShell.tsx` 主行结构 |
| ② | 是怎么修改的 | 复验确认 WP-A 留痕与源码不符：`footer` 仍与侧栏、内容列横向并列。把 `footer` 移进内容列、放在时间轴下方；内容列去掉 `border-r`（分割线是给假第三列用的） |
| ③ | 有什么作用 | 预览/时间轴吃满主行剩余宽度；「已保存 + 时刻」回到时间轴正下方 22px 底栏，不再浮在右列顶 |
| ④ | 对原工作流程的影响 | **不改**录/停/编/导操作。只改编辑器壳层占位。保存态与播放时刻含义不变 |

## 2026-09-08 · WP-A/WP-B 初步施工

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `EditorSidebar` 左轨重开/再点关闭；`EditorHeader` 删除右上侧栏钮；`EditorShell` 去掉内容列间隙并保留底栏在内容列底；`EditorPreviewPanel` 去掉预览横向 padding；`SliderControl` 改为 88/中间轨/52 三格；时间轴统一 8px 画布内边并修正 seek/标记坐标；编辑器 Select 统一 12px/28px/无阴影；`02` 更新状态 |
| ② | 是怎么修改的 | 左轨点击同时切页和打开可见状态；当前页再次点击仅关闭；滑轨只在中间网格格内定位；预览与时间轴改为无 gap 的连续内容列 |
| ③ | 有什么作用 | 修复 PR-01、PR-03、PR-06、PR-07、PR-08 的代码层主要根因，减少控件叠字和无效入口 |
| ④ | 对原工作流程的影响 | 录制/编辑/导出金路径不变；抽屉开关从顶栏迁到左轨，关闭后可从左轨恢复 |
