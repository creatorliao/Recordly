# 14 · 待确认代决策与待检验

> 方案已按推荐写入 [`03`](03-方案_素材库与时间轴合并.md)。要推翻点名即可。  
> 未听到「开始做」之前，**不改产品代码、不改手册、不提交 git**。  
> 本夹体量大、动金路径（编 + 导），属 **高价值高风险**：即使确认，也须分期；AI 不整包自动开工。

---

## 二、已代决（写入 03，要推翻点名）

| 编号 | 代决 | 含义 |
|------|------|------|
| M-A | **两层不混** | 侧栏「项目」格子继续 = 换课（草稿箱）。工程内另开「素材」层。不把现有格子改成拖上去就合并 |
| M-B | **合并 = 时间轴合成导出** | 源 mp4 / `.recordly` 不被 concat 成不可拆文件。不原样 #689 |
| M-C | **第一期只做顺序正片** | 一条正片轨、多段首尾相接（可插入、可删、可挪）。不做叠化、转场商店、多视频轨 |
| M-D | **只带媒体，不带对方工程的剪辑** | 从别的课拖/加入：只带画面+讲解声+光标遥测。不自动并入对方的缩放/字幕/文本框 |
| M-E | **画布跟当前工程** | 分辨率/帧率不同：上轨允许；较小一段按工程画布信箱。不静默拉伸。极端失败才 toast 拒绝 |
| M-F | **与 N5 共用模型、不同入口** | 本夹先把「多源 clip + 素材箱」模型设计落地；R14 的「编辑中再录」仍是另入口，不在本夹改 HUD，除非你点名同批 |
| M-G | **不换 OpenScreen V4 引擎** | 上游 ai-edition 已有素材箱，但本仓没有这套栈；整包搬 = 换编辑器。只借鉴口径（`05`）。要推翻须你点名「换引擎」 |

---

## 三、请你拍板（有问题就在这里回）

下面四条我**不能代你选**。回编号即可。

| # | 问什么 | 为何必须你说 |
|---|--------|--------------|
| Q1 | 第一期素材箱的来源：只要**本工程新录的 + 从本机选视频**，还是也要能「把侧栏另一个工程的成片拖进来」？ | 后者要读别人的 `videoPath` 和遥测，权限和心智都更重 |
| Q2 | 拖到时间轴的落点：默认**末尾**、默认**播放头**，还是像剪映那样看松手位置？ | 三种都常见；选错会觉得「拖了却跑到奇怪的地方」 |
| Q3 | 点素材缩略图：只预览、还是单击上轨、还是双击上轨？ | 和今天「单击 = 换课」不能打架；素材层必须另定 |
| Q4 | 值不值得在 **R14 壳层 / 02 视觉 / 03 轨道** 未收口前就开本夹施工？ | 本夹会动时间轴和导出。推荐：**先把壳和轨道皮收稳，再动多源**。若你这周就要拼课，可以说「视觉未完也先做模型」 |

---

## 四、确认后才真机看

| # | 看什么 | 为何人看 |
|---|--------|----------|
| T1 | 单段课：打开、切割、缩放、导出与今天一致 | 金路径回归 |
| T2 | 两段同分辨率：拖上轨 → 预览切点不黑、不串声 → 导出一条片 | 主场景 |
| T3 | 撤销「加入」后时间轴和素材箱都对 | 撤销语义 |
| T4 | 点项目格子仍换课；未保存仍先问 | 不能把草稿箱改坏 |
| T5 | 不同分辨率两段导出是否信箱、不变形 | 代决 M-E |
| T6 | GPU / CPU 两条导出都过切点 | 本机编码器 |

---

## 五、我不会在「开始做」之前做的

- 不改产品代码
- 不改使用手册
- 不提交 git
- 不把本夹工单插进 `R20260907-02` / `03` 的施工队列
