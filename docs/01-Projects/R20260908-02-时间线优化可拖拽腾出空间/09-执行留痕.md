# 执行留痕

## 本次实现

- 修改内容：时间线高度交互、折叠/展开、抓手视觉反馈、设置面板按钮文案，以及局部紧凑圆角样式。
- 修改方式：复用现有 `timelineHeight` 和 flex 布局，新增最小高度与折叠状态，不改时间线数据和播放逻辑。
- 作用：让预览区域在需要时自动腾出空间，提升时间线能力的可发现性，并消除左右面板控制语义歧义。
- 原流程影响：不改变录制、编辑数据、播放或导出流程；只增加可选的布局调整操作。
