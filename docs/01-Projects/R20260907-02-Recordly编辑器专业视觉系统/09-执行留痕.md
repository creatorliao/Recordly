# 09 · 执行留痕

## 2026-09-08 · 导出顶栏主按钮

| 要素 | 内容 |
|------|------|
| ① 改了什么 | `layout/EditorExportMenu.tsx` |
| ② 怎么改 | 幽灵文字链改为品牌蓝 `#2563EB` 实心按钮（高 24、白字、圆角 5），对标 OpenScreen 醒目导出；色用本仓蓝而非 OS 绿 |
| ③ 有什么作用 | 金路径终点在顶栏一眼可点 |
| ④ 对原流程的影响 | 仍先开下拉再选导出路径；不改编码 |

## 2026-09-08 · 播头头改倒三角

| 要素 | 内容 |
|------|------|
| ① 改了什么 | `src/components/video-editor/timeline/components/playhead/PlaybackCursor.tsx` |
| ② 怎么改 | 去掉 `rotate-45` 圆角方块、`shadow-lg`、`hover:scale-125`。改为向下尖角的 SVG 三角（10×8），热区 16×14，仍贴 2px `#2563EB` 线 |
| ③ 有什么作用 | 与剪映 / Premiere / 规范「小直角标、禁菱形光晕」一致，头不再像装饰菱形 |
| ④ 对原流程的影响 | 拖动/seek 不变。录→停→编→导不变 |

## 2026-09-08 · 编辑器工作区与设置面板优化（本轮）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | `layout/EditorHeader.tsx`、`layout/EditorPreviewPanel.tsx`、`layout/EditorShell.tsx`、`timeline/Item.tsx`、`SettingsPanel.tsx` |
| ② 怎么改 | 将比例选择与裁剪入口移到导出按钮同一行左侧；移除预览区重复工具行；预览画布改为在可用宽高内自适应；时间轴 item 内容层增加显式水平居中；设置抽屉统一收窄大圆角容器，保留必要的状态点和播放类圆形控件 |
| ③ 有什么作用 | 顶部减少一层占用，播放器在宽屏下利用更多左右空间；字幕时间轴文案的几何中心与条块一致；设置 Tab 更接近剪辑软件的专业、克制、低装饰视觉 |
| ④ 对原流程的影响 | 录→停→编→导主流程不变；比例和裁剪入口位置从预览上方移动到窗口顶部导出区左侧，功能行为不变；设置项语义与持久化不变 |

### 本轮验证

| 检查项 | 结果 |
|------|------|
| 定向 Biome lint（5 个本次修改文件） | 通过，0 error |
| TypeScript `npx tsc --noEmit` | 通过 |
| 全仓 `npm run lint` | 未通过，12 条既有 warning；未修改非本次文件 |
| `npm run typecheck` | 项目未定义该 script，改用 `npx tsc --noEmit` |
| 真机浅色/深色与 125%/150% | 待开发者在 Windows 完成手工走查，重点检查顶部控件、超宽预览与字幕条文字中心 |

## 2026-09-07 · 甲方案落地（WP1/WP2/WP3/WP5′/WP5″）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | `EditorWindow.tsx`、`editorTheme.css`、`EditorShell.tsx`、`EditorSidebar.tsx`、`SettingsPanel.tsx`、`AnnotationSettingsPanel.tsx`、`ExtensionManager.tsx`、`VideoEditor.tsx`、`useEditorUiState.ts`；时间轴 `EditorTimelinePanel.tsx`、`timelineLayout.ts`/test、`TimelineCanvas.tsx`、`Row.tsx`、`Item.tsx`、`ItemGlass.module.css`、`TimelineWrapper.tsx`、`PlaybackCursor.tsx`；版本 `package.json`/`package-lock.json` 升至 `1.4.0-beta.16` |
| ② 怎么改 | 编辑器专用 token 与 `.editor-scrollbar` 只从 `EditorWindow` 引入；工作区去外垫和 gap；抽屉统一 280px、直角、无投影、细滚动条；默认挂载为项目 Tab 且删除源就绪强制切画面；时间轴标尺 28、效果道 24、正片块 52、去 stretch/flex-1 空膛、柄 6、块 3px 实色、播放头去光晕 |
| ③ 有什么作用 | 对齐 Premiere/剪映/OpenScreen/VS Code 的专业密度；抽屉接缝稳定，项目进门落点明确，轨道行与短块更容易识别和拖拽；不改变切割、缩放、字幕、录制和导出算法 |
| ④ 对原流程影响 | 录→停→编→导动词不变；进入编辑器第一眼由画面改为项目列表，调画面需点左轨魔杖，这是 A1 已钉死的操作变化；未实施乙方案的默认讲解声条数、拖高、WP4 和死代码清理 |

## 2026-09-07 · 质量检查与留痕

| 检查项 | 结果 |
|------|------|
| 定向 Biome lint（18 个本次编辑器文件） | 通过，0 error |
| TypeScript `npx tsc --noEmit` | 通过 |
| 时间轴布局测试 | 通过，1 file / 4 tests |
| `npm run i18n:check` | 通过 |
| 全仓 `npm run format:check` | 未通过，发现 42 个工作区原有格式差异；未格式化或覆盖非本次文件 |
| 真机 T1–T8 | 待开发者在 Windows 浅色/深色、125%/150% 完成；本次代码已记录检查入口 |

## 2026-09-07 · 构建留痕

| 要素 | 内容 |
|------|------|
| 版本 | `1.4.0-beta.16` |
| Portable 命令 | `npm run build:win:portable` |
| 产物 | `release/recordly-portable-1.4.0-beta.16`；目录约 `1.1G`；入口 `electron.exe`（225,448,960 bytes）；SHA-256 `ec35fd19ccecc97ed5c7ed307ef6797d4288f95561e9f1e7b7b81012f605863a` |
| 提交 | 本次实施代码与留痕完成检查后提交；工作区既有原生 helper、package 与其他项目文档改动不纳入本次提交 |

## 2026-09-07 · 走查拆界：甲留本夹、乙开新夹（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 新增 `08`；修订 `00`/`01`/`03`/`04`/`07`/`14`；新建相邻夹 `R20260907-03` |
| ② 怎么改 | P0 + 咬死的 P1（图标/运输/播头/overlay）留甲；P1-1 默认一条讲解声与全部 P2 迁乙；P3 不进队列。甲乙都保持 A1、P01 波形能力、不改切割 |
| ③ 作用 | 第一刀能交出整页专业皮且点得中；不把「藏轨」和「改皮」缠在同一回退单元 |
| ④ 对流程 | 无（仍未改产品代码）。甲落地后进门第一眼变项目、正片在底；乙才可能少几行默认波形 |

## 2026-09-07 · 建夹 + 需求/分析/规范/方案（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 新建 PARA 夹 `R20260907-02`；写入 `00`–`04`、`14` |
| ② 怎么改 | 对照编辑器源码与口述；不改产品代码 |
| ③ 作用 | 把默认项目 Tab、抽屉接缝、去 AI 圆角立成可审方案 |
| ④ 对流程 | 无（未落地） |

## 2026-09-07 · 三视角走查与 P0 回写（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 新增 `07`；P0 回写 `03`/`04`/`14`（去 flex-1、禁空膛、split/16、默认一条讲解声、柄同号） |
| ② 怎么改 | 开发/设计/高频用户三条线走查，意见分 P0–P3 |
| ③ 作用 | 全面修复时按优先级施工，避免删 stretch 留下灰膛 |
| ④ 对流程 | 无（未改产品代码）。默认一条讲解声会改变「新课三行波形」，属可选可见性，不改切割 |

## 2026-09-07 · 轨道调查对账补记（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | `06` §六；`03`/`04` 取消「加 56px 标签列」；`14` 加 T-H |
| ② 怎么改 | 对上 OpenScreen 默认 392、V4 无标签列、本仓 6 行 stretch=3、sidebarWidth=0 |
| ③ 作用 | 全面修复时不误抄 392 / 不新开标签列 |
| ④ 对流程 | 无 |

## 2026-09-07 · 轨道纳入方案（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 新增 `06` 调查；`01` 补场景 F；`03` §11 + token；`04` WP5；`14` T-A～T-G 代决 |
| ② 怎么改 | 对照 Recordly `timelineLayout` / ItemGlass / 28px 柄，与 OpenScreen V4 行高 24/22/66、正片钉底、柄 6 |
| ③ 作用 | 全面修复时轨道和抽屉同一套专业皮；不抄 OS 的 11px 圆角和 AI |
| ④ 对流程 | 无（未改产品代码）。切割语义不变，行序变为正片在底（对齐剪映） |

## 2026-09-07 · 按推荐项完善并自审（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 钉死 A1/B1/C1/D1；修订 `03` `04` `14`；新增 `05` 审查账 |
| ② 怎么改 | 审查发现滑条配方、抽屉动画、预览列缝、注释板第三套皮等漏洞，直接改方案真源 |
| ③ 作用 | 避免按有缺陷的第一版开工 |
| ④ 对流程 | 无（仍等人「开始做」） |
