# 14 · 待确认代决策与待检验

> 推荐项 **已写入方案真源**（A1 / B1 / C1 / D1），审查漏洞已改 `03`/`04`。  
> 现在只等人一句：**开始做**。不再需要选编号。若要推翻某条，点名即可。

---

## 二、已钉死（原待选项）

| 编号 | 钉死 | 含义 |
|------|------|------|
| D-A → **A1** | 进入=编辑器窗挂载，默认项目；去掉源就绪跳画面 | 同一次打开里点了画面就停在画面；时间轴选中仍切板 |
| D-B → **B1** | WP1+WP2+WP3+WP5′+WP5″；WP4 与默认条数进乙 | 预览列外缝进 WP1；运输圆播放键不改 |
| D-C → **C1** | `editorTheme.css`，只在 `EditorWindow` import | 不做 `theme/` 目录 |
| D-D → **D1** | 抽屉统一 280 | 标题仍用文字按钮 |

审查中自行改定、不再问你的（见 `05`）：滑条改单行、抽屉槽瞬时、唯一 `EditorDrawer` 壳、注释板剥卡、不用 stable gutter、不叠 2px 缝。

轨道（后说，已按推荐写入 `03` §11 / `04` WP5 / `06`；要推翻再点名）：

| 编号 | 代决 | 含义 |
|------|------|------|
| T-A | 删 stretch，行高固定 | 面板仍可 min 160，行不再灌满变胖 |
| T-B | 效果 24 / 块 22 / 正片 52 / 音频 28 | 学 OS 瘦道，正片比 OS 66 略瘦 |
| T-C | 运输条留在预览下，瘦到 32 | 位置像剪映；不搬到时间轴头 |
| T-D | 正片钉底 | 像剪映 / OS |
| T-E | 柄 6 + 窄块外置 | 解决「不好点」 |
| T-F | 实色半径 3，不抄 OS 11px 卡、不藏条 | 和整页去 AI 味一致 |
| T-G | 本轮不做面板拖高、不抄默认 392 | 不过度 |
| T-H | 不加左轨名列 | 对齐 OS V4；省横向给时间 |
| T-I | 面板随内容，禁止 15% 空膛 | 走查 P0-2 |
| T-J | **迁出本夹** → `R20260907-03` | 默认一条讲解声是改可见行数，等甲把行高做对再做；本夹保持 P01 `showSourceAudio = true` |

---

## 三、确认后才真机看

| # | 看什么 | 为何人看 |
|---|--------|----------|
| T1 | 浅色：进编辑器是否项目、抽屉是否贴边、分割线是否单根 | 接缝截图像素级 |
| T2 | 画面 Tab 拉到底，细条是否出现 | 本机滚轮 |
| T3 | 开抽屉时预览是否只跳一次（播放中不连续挤） | 动画/性能 |
| T4 | 深色是否同一套瘦、方 | 主题 |
| T5 | 点时间轴片段、选中注释：属性是否仍在同一抽屉 | 回归 |
| T6 | 刚停录进编是项目；Launch 条没被带跑 | A1 + 范围 |
| T7 | 轨道：正片在底、行瘦、短块能拖；切割仍对 | WP5 |
| T8 | 125%/150% 下短块仍能拖、柄仍点得中 | P1-5 |

---

## 四、我不会在「开始做」之前做的

- 不改产品代码
- 不改使用手册
- 不提交 git
