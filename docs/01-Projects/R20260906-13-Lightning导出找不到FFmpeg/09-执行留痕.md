# 09 · 执行留痕

## 变更 1 · 专题落盘 + FFmpeg 路径改写 + 导出会话日志

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | 新建 `R20260906-13`（`00` / `01` / 本文件）；`electron/ipc/ffmpeg/binary.ts` + `binary.test.ts`；`electron/ipc/register/export.ts`（Lightning / 静态布局导出埋点）；`export.test.ts` mock；`R20260906-04/00` `10` 交叉引用 |
| **② 是怎么修改的** | `getFfmpegBinaryPath` / `getFfprobeBinaryPath` 复用 `rewriteAsarToUnpacked`，不再看 `isPackaged`。导出启动写 `export.ffmpeg.resolve` / `spawn` / `spawn-failed` |
| **③ 有什么作用** | Portable 默认 Lightning 不再 spawn 进 asar；日志能证明走的是 `app.asar.unpacked` |
| **④ 对工作流的影响** | 不改录→停→编→导步骤，不改导出按钮。旧 beta.9 zip 不会自己好，要重打包后再验 |

- 怎么验日志：见 `01` §4
