# 执行留痕

## 2026-09-08 · 按推荐落地：面板简化 + 画布拖拽

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | `webcamOverlay.ts`（反算）+ 单测；`VideoPlayback.tsx`（气泡可拖）；`EditorVideoPreview.tsx`（回写 setWebcam）；`SettingsPanel.tsx`（位置/素材 IA）；`zh-CN`/`en` `settings.json`；本夹 `00`/`14`/`09` |
| ② | 是怎么修改的 | 像素↔X/Y 互逆；`pointer-events`+grab；去掉自定义大开关；九宫格/精确/素材默认折叠；图贴与拖提示 |
| ③ | 有什么作用 | 主路径「预览拖一下」即可摆放；侧栏能力保留但不吵 |
| ④ | 对原工作流程的影响 | 不改变录→停→编→导；摄像头默认预设仍可用；导出仍走同一 `getWebcamOverlayPosition` |

## 2026-09-08 · 扩范围：整页简化方案（仍未改产品代码）

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | 重写/扩写 `00` `01` `02` `03` `14`；附图-02；本条 `09` |
| ② | 是怎么修改的 | 合并「画布拖」与「面板过复杂」两轮诉求；盘点 14 类控件；综合方案：拖为主、九宫格/精确滑条/素材默认收纳、补图贴、裁剪保留 |
| ③ | 有什么作用 | 确认前有完整需求/分析/方案；避免未拍板就改 `SettingsPanel` |
| ④ | 对原工作流程的影响 | **无**。录→停→编→导未改代码 |

## 2026-09-08 · 初版仅拖拽文档止（未改产品代码）

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | 立项夹初稿与附图-01 |
| ② | 是怎么修改的 | 只读几何与 pointer-events |
| ③ | 有什么作用 | 先钉拖拽可行性 |
| ④ | 对原工作流程的影响 | 无 |
