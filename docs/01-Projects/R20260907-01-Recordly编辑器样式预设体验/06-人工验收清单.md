# 06 · 人工验收清单

> 人收工后按项勾。执行智能体先用自动化覆盖能覆盖的，真机项不要假装勾过。  
> 项号可关联 WP / 代决。角色：培训师。平台：Windows。语言：先 zh-CN 再扫一眼 en。

---

## A · 找得到（入口）

| ID | 项 | 期望 | 代决 |
|----|----|------|------|
| A1 | 录完进编辑器，左轨画面 | 最上面是「样式预设」卡片 | D1 D8 |
| A2 | 左轨光标 | 同一张卡、同一列表、同一选中 | D8 |
| A3 | 顶栏导出左边 | **没有**书签/「预设」文字 | D2 |
| A4 | 项目 / 字幕 / 设置 / 摄像头 | **没有**这张卡 | D8 |
| A5 | 关掉侧栏再打开 → 画面 | 卡片还在 | D2 |
| A6 | 手册 | 写明入口搬家，旧「导出旁」不再当主说明 | WP5 |

## B · 取消 ≠ 删除

| ID | 项 | 期望 | 代决 |
|----|----|------|------|
| B1 | 未存过任何样式 | 第一行「未使用预设」选中；有空列表说明；导出可用 | — |
| B2 | 保存「冲冲冲」 | 该行打勾；未使用预设不再选中 | — |
| B3 | 点「未使用预设」 | 名字摘掉；**画面不变**；列表里冲冲冲还在 | D3 |
| B4 | ⋯ → 取消应用 | 与 B3 相同 | D3 |
| B5 | 没有任何 X 能删掉条目 | 删除只在 ⋯ 里 | P1 |
| B6 | ⋯ → 删除 → 对话框点取消 | 条目还在 | — |
| B7 | ⋯ → 删除 → 确定（当前条） | 条目没了；回到未使用；**画面不变** | D3 |

## C · 套用与误套

| ID | 项 | 期望 | 代决 |
|----|----|------|------|
| C1 | 新课点「冲冲冲」 | 壁纸/光标/缩放/画幅变 | D4 |
| C2 | 套用后看导出质量、GIF、字幕路径 | **与套用前相同** | D4 |
| C3 | 套用后看裁切、摄像头气泡 | **位置大小不变** | D4 D12 |
| C4 | 再点已选中且未改过的「冲冲冲」 | 无重复 toast、画面不闪 | — |
| C5 | 套用后立刻点 toast/卡片「撤销刚才的套用」 | 脸和选中都回到套用前 | D9 |
| C6 | Ctrl+Z | 不负责还原外观（时间轴照旧） | D9 |
| C7 | 套 A → 改阴影 → 套 B → 撤销刚才的套用 | 回到带阴影的 A，不是出厂 | D6 D9 |

## D · 脏、更新、另存、重命名

| ID | 项 | 期望 | 代决 |
|----|----|------|------|
| D1a | 套用后改阴影 | 「冲冲冲（已修改）」 | — |
| D1b | 点「更新「冲冲冲」」 | 无确认框；脏后缀消失；再开新课套这条是新阴影 | D11 |
| D1c | 另存为「测试」 | 多一条；当前变为测试 | — |
| D1d | 重名 | toast，不覆盖 | — |
| D1e | 空名保存 | toast，不存 | — |
| D1f | ⋯ 重命名 | 行内改名；Esc 取消 | — |
| D1g | 41 个字 | 输不进去或无法保存（max 40） | — |

## E · 工程与隔天

| ID | 项 | 期望 | 代决 |
|----|----|------|------|
| E1 | 套用后只保存课 | `.recordly` 里有 `appliedStylePresetId` | D5 |
| E2 | 关课再开同一课 | 仍显示该名字；**不要闪一次重套** | D5 |
| E3 | 新课 / 新录 | 未使用预设，不自动套上次 | D5 |
| E4 | 打开旧课（无该字段） | 未使用；不无故蓝点 | D10 |
| E5 | 工程 id 对应的样式已被删 | 未使用；不报错；不指纹挂到别人 | D10 |
| E6 | 未使用但脸碰巧与「冲冲冲」相同 | 提示碰巧一样，**不打勾成已应用** | — |
| E7 | 仅改已应用（例如取消应用） | 课名旁蓝点亮；保存后灭 | D5 |

## F · 金路径回归

| ID | 项 | 期望 |
|----|----|------|
| F1 | 录 → 停 → 编 → 导 | 不断、不改导出先选路 |
| F2 | 导出菜单 | 仍在顶栏右侧，流程与 14 一致 |
| F3 | 运动预设（专注/演示） | 还在设置/动画里，功能未删 |
| F4 | 画面各区块 Reset | 只重置该块；已应用则变已修改，不是取消应用 |
| F5 | en 界面 | 卡片英文 key 在，无中英混一行（本夹改过的字符串） |

## G · 自动化（执行方先跑）

| ID | 命令 | 期望 |
|----|------|------|
| G1 | `npm test` | 绿，含 look pick / hook / dirty 新测 |
| G2 | `npm run lint` | 绿 |
| G3 | `npm run typecheck` | 绿 |
| G4 | `npm run i18n:check` | 绿 |
| G5 | 搜 `EditorPresetMenu` | 0 处 |

---

勾法：真机过写「过」+ 日期；不过写现象，回到 09 留痕并修。不要把 G 项和真机项混成「都过了」。
