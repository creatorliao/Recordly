# 08 · 阶段 P02：导出 · 关窗 · 启动

> 目标：金路径壳层符合剪映/OBS/资源管理器习惯。  
> 队列：C1+L2、C2、C3、**N2（B4）**。条上「打开编辑器」留给 P03。  
> 前置：C2 避开与 `12` 抢 `main.ts`。B4 / P15 必须排在 B2 之后，禁止并行改 `main.ts`。

---

## 目标

1. 点开始导出 → **先**系统保存框 → 取消则不编码 → 确认后编码到该路径。  
2. 窗口 X **默认退出整个应用**；设置可改为最小化到托盘；「返回录制」仍出差。  
3. 设置里「启动时打开」「关闭窗口时」；三点只留动作（外观/语言/画质迁出）。

不改：录制条视觉密度（P03）、左轨宽度（P04）。

---

## 实施路径

| WP | 做 | 主要文件 |
|----|----|----------|
| B1 | 先选路再编码 + **原 A2**（Done / Path 中文）；默认课名；取消=不编 | 导出 hooks |
| B2 | 关窗默认退出 + Tray | `electron/main.ts`、`windows.ts` |
| B3 | 启动/关窗设置 + 三点迁出 | `MorePopover`、设置 store |
| B4 | **冷启动读 toggle**（原 P04-D3 / N2）：编辑器→最近项目 | `main.ts` 窗口创建（**B2 之后**） |

C3 原「条上打开编辑器」**不在本阶段做**（I1 / D13 / D14 属 P03-C3）。本阶段三点可暂留「打开项目」，P03 上条后三点去掉重复入口。

---

## 计划

1. B1  
2. B2（D7）  
3. B3  
4. B4  
5. 手册：导出、关 X、启动开关  
6. Gate-R。P15 右键另轨，推荐 B4 之后再开。

失败回退：B2 优先 `git revert` 该 PR。

---

## 验收标准

| ID | 标准 | 方式 |
|----|------|------|
| V1 | 取消保存框 → 无新导出文件、无编码进度；完成钮非死 `Done` | 真机 |
| V2 | 确认后文件在所选目录，名为课名（非法字符已消毒） | 真机 |
| V3 | 默认：编辑器或工具栏 X → **进程退出**，HUD 不复活 | 真机 |
| V4 | 「最小化到托盘」：X 后 Tray 在；菜单能开工具栏/编辑器/退出 | 真机 |
| V5 | 「返回录制」仍藏编辑器、亮工具栏，不等于 X | 真机 |
| V6 | 设置能写下启动/关窗；**冷启动跟 toggle**（B4，本阶段） | 重启真机 |
| V7 | 三点无外观/语言/采集画质 | 真机 |
| V8 | 手册速查卡已写「原来怎样、现在怎样」 | 文档 |
| V9 | 自动化四件套绿 | 自动 |

In：上表。Out：条 CSS、侧栏项目格子、右键注册表（P15）。
