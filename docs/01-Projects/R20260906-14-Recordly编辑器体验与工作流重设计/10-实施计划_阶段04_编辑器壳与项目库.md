# 10 · 阶段 P04：编辑器专业壳与项目库

> 复盘后：本阶段**不改** `main.ts`（冷启动已迁 P02-B4）。  
> 队列：D1a（D17+F3+F5）→ D1b（D12+F1+F2）→ D1c（F4）→ D2（N1）。  
> D1a 可与 P03 并行。数字认 `D:\code\vscode` @ `3d7cfab`。

---

## 目标

打开编辑器：顶栏是 VS Code 那种文字菜单（高 30）；左轨 48、上工具下齿轮、选中 2px 条；底栏 22；项目在侧栏格子。

不改：录制条、关窗、冷启动、追加录制。

---

## 实施路径

| WP | 做 | 文件 |
|----|----|------|
| D1a | 顶栏菜单条 + 开关侧栏 + 文件「最近打开」10 条；预设/导出改文字 | `EditorHeader`、两个菜单触发器、`EditorTitlebar.module.css` |
| D1b | 左轨 48、底钉齿轮、2px 条、去圆点 | `EditorSidebar.tsx` |
| D1c | 状态栏高 22：未保存 / 时码 | 新小组件 + `EditorShell` |
| D2 | 项目 2 列网格上侧栏；打开走文件菜单 | `ProjectBrowserDialog`、`EditorShell` |

---

## 计划

1. D1a → 对照 [`12`](12-对照_VSCode官方顶栏源码事实.md) 抄，不目测  
2. D1b → 对照 [`13`](13-需求与方案_VSCode再抄五条.md) F1 F2  
3. D1c  
4. D2  
5. 手册：项目入口、文件菜单  
6. Gate-R  

---

## 验收

| ID | 标准 |
|----|------|
| V0 | 顶栏文字菜单，高 30，无蓝导出胶囊；diff 无 HUD |
| V1 | 齿轮在左下，只开设置；上段是工具 |
| V2 | 选中 2px 条，无圆点列 |
| V3 | 顶栏能关/开设置板 |
| V4 | 文件 → 最近打开 ≤10，能进课 |
| V5 | 底栏 22px，有未保存或时码 |
| V6 | 侧栏格子能开课 |
| V7 | 自动化四件套绿 |

In：上表。Out：`main.ts`、R-HUD、N5。
