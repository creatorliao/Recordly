# 11 · 阶段 P05：时间轴声画字

> 目标：开录前知道麦在不在；字幕点了能改；文本进出有淡出。  
> 队列：N3、N4、C4（L4 已在 P01 做「按时段收」）。  
> 前置：无硬前置。麦电平若挂在条上，最好 P03 分组稳定后再做 E1，避免刚排完的条再挤一次。

---

## 目标

- 录制工具栏有**麦活波形/电平**（#702 最小集），开录前能判断有没有声。  
- 时间轴或预览点一条字幕就能改字（不碰 Whisper 引擎 / 不下载模型，口径仍走 `R20260906-08`）。  
- 文本框进出复用字幕已有 fade/rise/pop，默认淡出。

不进本阶段：N5 追加、N6 降噪、完整调音台。

---

## 实施路径

| WP | 做 | 主要文件 |
|----|----|----------|
| E1 | HUD 麦钮旁或齿轮内最小电平；复用已有 audio level 组件若有 | `audio-level-meter.tsx`、`LaunchWindow.tsx` / 齿轮 |
| E2 | 点选字幕进编辑；文本框默认淡出接 `captionLayout.ts` | `CaptionListPanel` / 时间轴、`VideoPlayback.tsx` |

E1 若必须改条布局：只加电平，不回退 R-HUD 数字。

---

## 计划

1. E1（G3：听得见）  
2. E2（G3：改得了字 + C4 淡出）  
3. 阶段 Gate-R  
4. 本夹五阶段收口：更新 `00-README` 进度一行；N5/N6 仍写「另点头」

手册：波形/电平补一句即可；点选改字若入口变了则必更。

---

## 验收标准

| ID | 标准 | 方式 |
|----|------|------|
| V1 | 对麦说话时条上电平会动；静音则不动 | 真机 |
| V2 | 时间轴或预览点字幕能改字并写回 | 真机 |
| V3 | 文本框进出有淡出；播放过头仍收（P01-V5 不回退） | 真机 |
| V4 | 未触发模型下载、未改 Whisper 路径 | 走查 |
| V5 | 自动化四件套绿 | 自动 |

In：上表。Out：追加到时间轴、RNNoise、#689 拼接。

---

## 五阶段之后（不施工）

| 项 | 何时再看 |
|----|----------|
| N5 追加录制 | 你点头；时间轴新 clip 接末尾（D1）；不原样 #689 |
| N6 降噪 | 人工；默认关 |
| 竖条 / 提词器 / 光标模式 | 不进本夹 |
