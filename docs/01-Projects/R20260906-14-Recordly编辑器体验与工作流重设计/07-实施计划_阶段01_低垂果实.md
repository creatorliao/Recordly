# 07 · 阶段 P01：低垂果实

> 目标：编辑器立刻少摩擦，不改启动/关窗/录制条。  
> 队列：L1–L6。前置：无。可与 `12` 保命并行。

---

## 目标

培训师打开已有课时：缩放面板是中文、删除缩放只有一颗键、字幕不像西文怪体、播放过头文本框会收、时间轴看得到讲解声、Delete 能删当前选中。

不改：录制条布局、导出选路顺序、关窗、项目库位置。

---

## 实施路径

| WP | 做 | 主要文件（调查所得，施工时再核对） |
|----|----|--------------------------------------|
| A1 | 删区块内「删除缩放」；补 `zoom.globalSettings` / `effects.classicZoom`；可选去顶栏 1.5x 徽章 | `SettingsPanel.tsx`、i18n json |
| A3 | 默认中文栈字体；字幕面板补字体选择；文本占位 i18n；播放中出时段即收（暂停选中仍钉住） | `captionStyle.ts`、`VideoPlayback.tsx`、字幕/文本设置面板 |
| A4 | 新录制 `showSourceAudio = true`；Delete 删当前缩放/字幕/文本（别误删整段 clip） | `editorPreferences` / 新项目默认、`useEditorGlobalInteractions.ts` |

每 WP：步骤 0 能跑的测试 → 改 → `npm test` / lint / typecheck / i18n:check → 本仓习惯下等人说再 commit。

---

## 计划

1. A1  
2. A3（字体 + 时段）  
3. A4（波形默认 + Delete）  
4. 阶段 Gate-R  

**A2 已并入 P02-B1**（导出文案和先选路同一批文件，不在本阶段单开）。

手册：本阶段不改操作习惯，**不必**改使用指南（L5 补一句「新项目默认有波形轨」即可，可并入 A4 提交说明）。

---

## 验收标准

| ID | 标准 | 方式 |
|----|------|------|
| V1 | 简中缩放面板无 ANIMATION / Classic Animation | 真机 + i18n:check |
| V2 | 缩放删除键只有底栏一颗 | 真机 |
| V3 | （原导出 Done/Path：改到 P02-B1 验收） | — |
| V4 | 新字幕默认微软雅黑栈（英文 locale 可用 Segoe 在前） | 代码 + 真机 |
| V5 | 播放越过文本时段，选中也消失；暂停选中仍可拖 | 真机 |
| V6 | 新录制时间轴出现源音频波形 | 真机 |
| V7 | Delete 删当前选中缩放/字幕/文本，不删整段视频 | 真机 + 单测若有 |
| V8 | lint / typecheck / test / i18n:check 绿 | 自动 |

In：上表。Out：HUD CSS、`electron/main.ts` 关窗、导出选路顺序。
