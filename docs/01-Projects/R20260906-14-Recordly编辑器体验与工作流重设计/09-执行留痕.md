# 09 · 执行留痕

> 真源：`03-方案_综合完善方案.md` / `06-实施计划_00-路线图总览.md`。每改一处记四要素。
> 分支：`dev-creator`（不加新分支）。每 WP 一个提交，可 `git revert` 单点回退。

---

## 变更 1 · P01-A1 缩放中文 key + 去区块内重复删除键

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `src/i18n/locales/en/settings.json`、`zh-CN/settings.json`；`SettingsPanel.tsx` 缩放区块 |
| **② 是怎么修改的** | 补 `zoom.globalSettings` / `effects.classicZoom`（en+zh-CN）；删掉缩放区块内那颗 `zoom.deleteZoom`，只留底栏一颗 |
| **③ 有什么作用** | 简中不再出现 ANIMATION / Classic Animation 英文兜底；「删除缩放」只有一颗 |
| **④ 对工作流的影响** | 不改录→停→编→导；仅面板文案与去重 |

- commit：`a3619fef`

## 变更 2 · P01-A3 字幕/文本中文字体、字体选择、占位中文、播放收起

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `types.ts`、`AnnotationSettingsPanel.tsx`、`SettingsPanel.tsx`、`VideoPlayback.tsx`、`useAnnotationRegionCommands.ts`、`editor.json`(en/zh-CN) |
| **② 是怎么修改的** | 字幕/文本默认字体从 SF Pro 改为 `"Microsoft YaHei UI", "Segoe UI", sans-serif`；字幕面板补字体下拉（复用文本字体列表并加微软雅黑）；新增文本占位走 i18n `annotations.textPlaceholder`；播放中选中文本框出了时段即收、暂停仍钉住 |
| **③ 有什么作用** | 中文不再方框/西文怪体；字幕可换字体；不再硬编码 Enter text...；文本框播放不赖屏 |
| **④ 对工作流的影响** | 默认字体变化只影响新建字幕/文本；旧项目仍用已存字体。不改操作步骤 |

- commit：`5fac1ca4`

## 变更 4 · P02-B1 导出先选路再编码 + Done/Path 中文

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/ipc/register/export.ts`、`preload.ts`、`electron-env.d.ts`；`useExportRunner.ts`、`useExportDialogActions.ts`、`useEditorExportController.ts`、`VideoEditor.tsx`、`EditorExportMenu.tsx`、`editor.json` |
| **② 是怎么修改的** | 新增 `choose-export-path` IPC（编码前弹系统保存框、默认课名、记住上次目录）；`handleExport` 接受 `outputPath` 直写所选路径；完成钮走 `common.actions.done`；进度 `Path:` 改 `编码:` |
| **③ 有什么作用** | 取消保存框则不编码；确认后直接编到所选路径；不再先写 temp 再搬家 |
| **④ 对工作流的影响** | 改默认：导出先选路径再干活（对齐剪映/OBS）。取消不再产生文件 |

- commit：`5a1465b5`

## 变更 5 · P02-B2 关窗默认退出 + Windows Tray

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/main.ts`（shouldUseTray、编辑器/HUD close 处理、updateTrayMenu） |
| **② 是怎么修改的** | `shouldUseTray` 覆盖 Windows；编辑器 X 默认 `app.quit()`、`closeWindowBehavior=tray` 时 `hideWindowToTray`（首次气泡）；HUD 的 X 同规则；Tray 菜单中文（打开录制工具栏/打开编辑器/退出） |
| **③ 有什么作用** | 关 X 默认退出（不再复活 HUD）；可选托盘；Tray 能进任一面 |
| **④ 对工作流的影响** | 改默认：X=退出。选「最小化到托盘」后 X 才藏到托盘。「返回录制」仍独立 |

- commit：`a598ce67`

## 变更 6 · P02-B3 启动/关窗设置 + 三点只留动作

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `MorePopover.tsx`、`LaunchWindow.tsx`、`SettingsPanel.tsx`、`editor.json` |
| **② 是怎么修改的** | 三点删外观/语言/采集画质；编辑器设置页新增「启动时打开」「关闭窗口时」两档切换（持久化 app settings）；采集画质迁到编辑器设置页（读写 recording preferences） |
| **③ 有什么作用** | 设置进设置；三点只留动作 |
| **④ 对工作流的影响** | 外观/语言/画质入口从三点挪到编辑器设置；启动/关窗新增可选项（默认与旧一致） |

- commit：`f3f8f09f`

## 变更 7 · P02-B4 冷启动读 toggle + 最近项目

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `electron/main.ts`（cold start 分支、openMostRecentProjectIfAvailable） |
| **② 是怎么修改的** | `whenReady` 末尾按 `startupScreen` 分支：editor 模式先 `loadRecentProjectPaths` 找最近一条有效项目并 `setCurrentProjectPath`，再 `createEditorWindowWrapper` |
| **③ 有什么作用** | 冷启动跟「启动时打开」toggle；编辑器模式自动开最近项目 |
| **④ 对工作流的影响** | 默认仍录制工具栏；拨到编辑器后才开最近项目（没有则空态） |

- commit：`0ff1bc16`

## 变更 8 · P03-C1 R-HUD 条密度（钮34/垫7/间距5/圆角12）

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `LaunchWindow.module.css`（.bar/.barStateViewport/.barState/.sep/.recBtn/.recDot/.stopSquare） |
| **② 是怎么修改的** | 圆角 20→12、垫 11→7、间距 10→5；view 46→34；录制键 46→34、点/方块 11 |
| **③ 有什么作用** | 去掉胖胶囊感，对齐 R-HUD D11′；条皮仍走主题变量不写死色 |
| **④ 对工作流的影响** | 不改录→停→编→导；仅浮层条密度。拖动/穿透逻辑未碰 |

- commit：`7b6dfa04`

## 变更 9 · P03-C2/C3 系统声上条 + 录制键右侧「打开编辑器」

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `LaunchWindow.tsx`、`MicPopover.tsx`、`launch.json` |
| **② 是怎么修改的** | 系统声音从 MicPopover 提升为条上独立开关（SpeakerHigh/SpeakerX）；未录时录制键右侧加「打开编辑器」（MagicWand→switch-to-editor）；录中不渲染 |
| **③ 有什么作用** | 系统声一键在条上；条能直接进编辑器（I1/D13/D14） |
| **④ 对工作流的影响** | 新增「打开编辑器」入口（录中隐藏）；麦 popover 不再含系统声 |

- commit：`b129b2e9`

## 变更 10 · P03-C2 源去灰框 + 倒计时收进设备齿轮

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `LaunchWindow.tsx`、`launch.json` |
| **② 是怎么修改的** | 源选择从 outline 灰框胶囊改为 ghost 图标+短名（I3）；倒计时按钮改设备齿轮（GearSix，D15/V5） |
| **③ 有什么作用** | 去灰框；条上无单独秒表钮，倒计时在齿轮里设定 |
| **④ 对工作流的影响** | 不改录→停→编→导；仅条上入口视觉 |

- commit：`15e2fdac`

## 变更 11 · P04-D1a/D1b 顶栏菜单条 30 + 左轨 48 底齿轮 2px 条

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `EditorHeader.tsx`、`EditorSidebar.tsx`、`EditorShell.tsx`、`EditorExportMenu.tsx`、`useEditorUiState.ts`、`editor.json` |
| **② 是怎么修改的** | 顶栏图标砖改 VS Code 文字菜单条（文件/编辑，高 30，文件含最近打开≤10）；导出改文字去蓝胶囊；右侧侧栏开关（F3）；左轨 48、齿轮钉底、选中 2px 条、图标 24 |
| **③ 有什么作用** | 编辑器看起来像专业工具；左轨底齿轮只开设置 |
| **④ 对工作流的影响** | 顶栏能力收进文字菜单（返回录制/保存/最近打开都在文件菜单）；侧栏可开关 |

- commit：`7a7b710f`

## 变更 12 · P04-D1c/D2 状态栏 22 + 侧栏项目格子

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `EditorShell.tsx`、`types.ts`、`ProjectsPanel.tsx`(新)、`EditorSidebar.tsx`、`editor.json` |
| **② 是怎么修改的** | 底部 22px 状态栏（未保存/已保存 + 当前/总时长）；侧栏新增「项目」格子（封面+课名+当前高亮，复用项目库 24 条） |
| **③ 有什么作用** | 底栏 22；项目从浮层升成左侧第一栏，导入走文件框 |
| **④ 对工作流的影响** | 项目入口从顶栏浮层改为侧栏第一栏；打开项目能力不丢 |

- commit：`d5d09e76`

## 变更 13 · P05-E1/E2 条上麦电平 + 文本框进出淡出

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `LaunchWindow.tsx`、`AnnotationOverlay.tsx`、`VideoPlayback.tsx` |
| **② 是怎么修改的** | HUD 麦钮旁加实时电平（复用 useAudioLevelMeter/AudioLevelMeter）；文本框 180ms 进出淡出 |
| **③ 有什么作用** | 开录前能判断麦在不在；文本进出淡出不闪切 |
| **④ 对工作流的影响** | 不改步骤；点选改字经核查已可用（CaptionListPanel + 时间轴选择） |

- commit：`7a2c66c7`

## 变更 14 · P15-S0/S1/S2 资源管理器右键级联

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `windowsExplorerContextMenu.ts`(新)、`main.ts`、`preload.ts`、`electron-env.d.ts`、`SettingsPanel.tsx`、`editor.json`、`build/installer.nsh`(新) |
| **② 是怎么修改的** | 按设置（默认开）写/删 HKCU 三棵级联（文件/目录/目录空白处），三动词；设置页 Windows 集成开关；argv 解析分发；卸载删键 |
| **③ 有什么作用** | 资源管理器右键直接进课/开录/开工具栏 |
| **④ 对工作流的影响** | 新增右键入口（默认开，可关）；开录用上次源。S3 手册一句待补（手册文件正被并行进程改动，避免冲突） |

- commit：`e3ddf0f6`

## 变更 3 · P01-A4 新录制默认显示源音频波形轨

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `useTimelineProjection.ts` 初始自动整轨 clip |
| **② 是怎么修改的** | 新录制的自动整轨 clip 加 `showSourceAudio: true` |
| **③ 有什么作用** | 新录制时间轴直接出现讲解声波形轨（此前默认关，能力在却看不见） |
| **④ 对工作流的影响** | 不改录→停→编→导；可在片段面板关闭波形。Delete 删选中缩放/字幕/文本经核查已存在，未重复实现 |

- commit：`2f29b4a5`

## 变更 15 · 条上打开编辑器进空项目 + 去掉条上空白

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `useInitialEditorSource.ts`、`editorEmptyWorkspace.ts`（新）及测试；`EditorVideoPreview.tsx`、`ProjectsPanel.tsx`、`EditorSidebar.tsx`、`EditorShell.tsx`、`VideoEditor.tsx`、`useEditorProjectController.ts`、`useProjectOpenActions.ts`；`LaunchWindow.tsx`；`editor.json` en/zh-CN；手册 `R20260906-01` 的 `02`/`05`；本夹 `14`/`00` |
| **② 是怎么修改的** | 没有当前项目/会话/视频时不再 `setError(noVideoToLoad)`，改为空项目并切到「项目」选项卡、打开侧栏；预览不挂空 src 的 VideoPlayback；项目格子加「新建录制」和空态「上次」角标；打开/导入成功切回「场景」。条上删除隐藏 `ProjectPopover`；三点「打开项目」改为 `switchToEditor` |
| **③ 有什么作用** | 未录点「打开编辑器」进完整编辑器选历史课或新建，不再整页红字；魔杖钮旁不再多一块空白 |
| **④ 对工作流的影响** | 录→停→编→导不变。未录时点打开编辑器：从拦截页改为完整编辑器 + 项目选项卡。有当前课仍直接打开这一课 |

- 方案：[`14`](14-问题_录制条打开编辑器空态与条上空白.md)
- 版本：`1.4.0-beta.11` → `1.4.0-beta.12`（`npm version --no-git-tag-version`）
- 产物：`D:\code\Recordly\release\recordly-portable-1.4.0-beta.12\`（已核验 `electron.exe`；无 zip）

## 变更 16 · 去掉魔杖与三点之间的分隔线空位

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `LaunchWindow.tsx` idle 条：打开编辑器与三点之间的 `Separator` |
| **② 是怎么修改的** | 删掉该分隔线。隐藏 `ProjectPopover` 去掉后，这条线 + `mx-[5px]` + 条上 `gap:5` 在浅色条上仍像一颗空白按钮 |
| **③ 有什么作用** | 录制键 → 打开编辑器 → 三点 间距与其他图标钮一致 |
| **④ 对工作流的影响** | 不改录→停→编→导；只修条上视觉 |

- 版本：`1.4.0-beta.12` → `1.4.0-beta.13`
- 产物：`D:\code\Recordly\release\recordly-portable-1.4.0-beta.13\`（已核验 `electron.exe`）

## 变更 17 · P03b 拍板 / 录制键 / 选源后开录 / 竖条 / 笔记提词器

| 维度 | 记录 |
|------|------|
| **① 修改了哪些内容** | `HudIcons.tsx`（新）；`LaunchWindow.tsx` / `.module.css`；`RecordingControls.tsx`；`NotesWindow.tsx` `NotesToolbar.tsx` `notesTeleprompter.ts`+测试 `NotesWindow.css`（新）；`App.tsx`；`electron/windows.ts` `preload.ts` `electron-env.d.ts`；`launch.json`/`common.json` en+zh-CN；手册 `R20260906-01` 的 `02`/`05` |
| **② 是怎么修改的** | 打开编辑器改拍板 SVG；录制键透明底+红圆/录中方块带时；没源点录制先选源并在选完后开录；条上横竖切换（`hudTrayLayout`）；整套笔记窗（Tiptap+提词滚动/字号/镜像）+ `setContentProtection`；条上图标与 OpenScreen HUD 线标对齐 |
| **③ 有什么作用** | 条不再「大红盘+魔杖」；选源少点一次；竖条可躲开屏幕下沿；录时可看讲稿且不进成片 |
| **④ 对工作流的影响** | 金路径动词不变。操作变化：打开编辑器认拍板；没源点录制会先选再录；多了笔记和竖条（默认可不用） |

- 方案：[`17`](17-方案_第二轮可移植点.md)
- 版本：`1.4.0-beta.12` → `1.4.0-beta.14`（`npm version --no-git-tag-version`；跳过已占用的 beta.13 目录包）
- 产物：`D:\code\Recordly\release\recordly-portable-1.4.0-beta.14\`（已核验 `electron.exe`）
- 验证：`notesTeleprompter` 15 项绿；launch 单测 27；`i18n:check` 绿；`tsc` 绿。真机浅/深、选源取消、笔记滚动待人走查 |
