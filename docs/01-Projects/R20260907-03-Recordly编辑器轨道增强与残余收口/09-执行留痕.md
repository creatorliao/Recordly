# 09 · 执行留痕

## 2026-09-07 · WP-A～WP-D 实施及布局补强

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 默认源音频行改为 mixed 优先、否则 mic 一条；轨道块文字与图标使用容器查询单位随块宽缩放并居中；时间线增加 160–560px 可拖高度、双击恢复默认；压缩预览顶部比例/裁剪工具行、播放区与时间线间距；Settings 指定胖按钮/下拉改 28px + 2px 半径；删除无引用 `TimelineToolbar.tsx` |
| ② 怎么改 | 保留全部 source audio 元数据和设置面板入口，只把默认可见集合与属性设置解耦；布局分割条使用 pointer events，不引入第二套持久化偏好；不改 A1、切割、波形算法、Launch、对话框和 WP-E |
| ③ 有什么作用 | 窄轨道中的文字不会固定大字号溢出；笔记本屏幕减少顶部和中间留白，时间线可按长字幕课手动加高；残余控件与甲方案视觉 token 对齐 |
| ④ 版本 | `package.json` / `package-lock.json` 升至 `1.4.0-beta.17` |

## 2026-09-07 · 本轮验收入口

| 检查项 | 结果 |
|------|------|
| TypeScript `npx tsc --noEmit` | 通过 |
| 定向 Biome lint（本轮编辑器文件） | 通过 |
| 源音频轨道测试 | 通过，6 tests |
| 完整测试 | 通过，130 files / 1145 passed / 1 skipped |
| Windows Portable | 通过，`release/recordly-portable-1.4.0-beta.17`；入口 225,448,960 bytes；SHA-256 `f9bd3d86b461956c2f1b1e2481a01cf38a0f45e22d2275514336b5ba28444a2d` |
| 人工走查 | 待开发者在笔记本分辨率、浅色/深色、125%/150% 下检查轨道文字、拖高/恢复、预览密度与 WP-E 边界 |

## 2026-09-07 · 从甲夹拆出本夹（文档）

| 要素 | 内容 |
|------|------|
| ① 改了什么 | 新建 PARA 夹 `R20260907-03`；写入 `00`–`03`、`14`、本文 |
| ② 怎么改 | 承接甲夹走查 P1-1 与全部 P2；写清对甲的依赖、与 R14 P01 / A1 的边界 |
| ③ 作用 | 甲完成后可单独执行，回退不缠皮 |
| ④ 对流程 | 无（未改产品代码）。落地后才可能改默认波形行数、可选拖高 |
