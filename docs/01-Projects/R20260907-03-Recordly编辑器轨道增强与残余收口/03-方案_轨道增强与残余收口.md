# 03 · 方案：轨道增强与残余收口

> **方案真源（乙）**。甲未完成、未获「开始做」前不改产品代码。  
> 规范半径/密度继续吃甲夹 [`03`](../R20260907-02-Recordly编辑器专业视觉系统/03-规范_编辑器专业视觉系统.md)，本夹不另开视觉系统。

---

## 一、已钉

| 编号 | 决定 |
|------|------|
| F-A | 默认只铺 **一条** 讲解声行：优先 `mixed`，否则 `mic`；`system` 不默认铺。`showSourceAudio` 默认 **仍 true** |
| F-B | 可拖高：下缘手柄，范围约 160–560（学 OS 数字，不抄默认 392）；默认无覆盖 = 甲的随内容 |
| F-C | WP4 残余：Settings 散落大按钮、导出/预设菜单圆角吃 `--ed-radius-control`（2）。对话框体系不动 |
| F-D | 运输条稳定后删除 `TimelineToolbar.tsx`；hint 收到运输条右侧一行 10px；扫残余 Tab `AnimatePresence` |
| F-E | 藏扩展 Tab：**默认不做**。要做须点名，并改手册 |

每 WP ≤500 行。SettingsPanel 禁止整文件 `rounded-*` 替换。

---

## 二、施工

### WP-A · 默认一条讲解声（P1-1）

| 做 | 文件（落地时复核） |
|----|-------------------|
| 默认展示集：`mixed` 或 `mic` 一条 | `sourceAudioTracks.ts` 或 `TimelineCanvas.tsx` 的 map 过滤 |
| 片段属性仍列出全部旁路，勾上才加行 | `SettingsPanel` / 片段音频区（只接线，不扫皮） |
| 窄测：有 system+mic 时默认行数 = 1；勾 system 后 = 2 | `sourceAudioTracks.test.ts` 等 |

**不做：** 把 `showSourceAudio` 改 false；删 sidecar；改波形峰值算法。

### WP-B · 轨道区可拖高（P2-1）

| 做 | 文件 |
|----|------|
| 时间轴上缘或与预览的分割可拖 | `EditorTimelinePanel.tsx` / `EditorShell.tsx` |
| 写入会话记忆（或已有 editor 偏好）；新课无值 → 随内容 | 现有 persistence，不新造第三套 |
| 双击分割恢复随内容 | 同一处 |

**不做：** 默认高度改 392；加回 `flex-1` / stretch。

### WP-C · 残余圆角（原 WP4 / P2-4）

| 做 | 文件 |
|----|------|
| Settings 散落 `h-10 rounded-xl` → 高 28、半径 2 | 只点名的按钮 class，不整文件 |
| 导出菜单、预设下拉圆角 | 顶栏菜单触发器；**不改** `R20260907-01` 的应用/取消/删除 |
| 字幕生成等大键 | 同半径 |

**不做：** 对话框 `--radius`、Launch、成片摄像头预览圆角。

### WP-D · 死代码与 hint（P2-3 / P2-5 / P2-6）

| 做 | 文件 |
|----|------|
| 确认无引用后删除 `TimelineToolbar.tsx` | 及 barrel |
| 运输条右侧 10px 快捷键一行，不造 chip | `EditorPreviewPanel.tsx` |
| 扫抽屉 Tab 残余 y/blur `AnimatePresence` | `EditorSidebar` 一带 |

### WP-E · 藏扩展入口（P2-2，默认跳过）

空态甲已收紧。本 WP 仅在点名后：左轨去掉扩展图标，设置里留「开发者 / 扩展」。改手册。

---

## 三、验收

| ID | 标准 |
|----|------|
| E1 | 新课时间轴默认 1 条讲解声；属性能开出 system |
| E2 | `showSourceAudio` 仍默认真；关掉则 0 行 |
| E3 | 切割 / 点块出属性 / A1 进门项目 与甲完成时一致 |
| E4 | 拖高后预览变矮；双击或新课回到随内容；无底空膛 |
| E5 | 散落按钮与导出菜单半径 2；Launch 外观不变 |
| E6 | 仓库内无 `TimelineToolbar` 引用 |
| E7 | 四件套绿 |

---

## 四、回退

- WP-A 单独 revert（只回默认行数）
- WP-B / WP-C / WP-D 按文件回
- 禁止用本夹 revert 甲的 stretch / 抽屉 / A1

---

## 五、手册

落地后 `R20260906-01` 补：默认一条讲解声怎么开齐旁路；轨道怎么拖高。WP-E 若做再补扩展入口。
