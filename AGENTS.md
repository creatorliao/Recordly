# AGENTS.md — Recordly 工作原则与工作流程

> 本文档是 AI 在本仓库工作的总纲：**目标、原则、流程、决策模型、留痕规范**全部在此。
> 任何改动前先读本文件，判断「该不该做、按什么顺序做、做完怎么记录」。

---

## 一、项目目标

| 项 | 内容 |
|----|------|
| 角色 | 培训师，录制软件操作过程 |
| 核心路径 | 启动录制 → 停止 → 编辑 → 导出视频（全程必须稳定可用） |
| 三大诉求 | ① 减轻剪辑工作量 ② 字幕/配音 ③ 不过度设计 |
| 现阶段焦点 | **仅 Windows**（Linux / macOS-only 一律不做） |
| 语言范围 | 只关注**简体中文 + 英文**，不为其他语言做额外修复 |
| 工作分支 | **`dev-creator`**：本地移植社区提交，不代合、不代关上游 PR |
| 本机分发 | 要能打 Windows 安装包；进程/供应商身份按内部环境要求处理（避免未备案厂商名被拦截） |

**目标锚点（G1–G4）**：任何改动必须先落到其中一个目标，落不进就不做。

| 编号 | 目标 | 衡量 |
|------|------|------|
| G1 | 完整路径可用 | 录→停→编→导 无阻断 |
| G2 | 减轻剪辑工作量 | 录完到导出 ≤15 分钟 |
| G3 | 字幕 / 配音 | 一键字幕、讲解声可靠 |
| G4 | 体验良好、不过度设计 | 功能少而稳 > 大而危 |

---

## 二、核心工作原则（八条，缺一不可）

1. **功能完善**：核心路径每一步都稳定可用，先保「录得稳、点得动、听得清、存得了、导出顺」。
2. **不过度设计**：宁可少而稳，不要大而危。改动 **>500 行**进入高审慎，**>2000 行**基本拒绝，除非能拆小。
3. **体验良好**：界面/交互正确顺滑，中文体验优先。
4. **风险优先（低风险优先）**：先做低风险、高确定性的修复；低垂果实天然最前。
5. **工作留痕**：每次修改必须记录四要素（见 §六），可追溯、可回退。
6. **关联与冲突前置识别**：合并前分析功能间的关联/依赖/冲突，尤其「有了 A 就没了 B」的互斥情况，**先记录并与用户确认再动手**。冲突先判断能否**共存设计**（如「默认 A + 可选 B」），只有真互斥才二选一。
7. **用户视角、不破坏现有体验**：所有功能增强必须站在用户角度，**不改变核心工作流程（录→停→编→导）、不破坏原有体验**；新增功能默认「可选」，老用户无需改变操作。
8. **符合大多数人的工作习惯、符合大多数软件的使用习惯**：默认交互对齐培训师已经会的软件（剪映 / CapCut、OBS、Camtasia、Windows 资源管理器、Office），不要发明只有本软件才懂的迷宫。对照口诀：关窗≈结束这次、导出先选路径再干活、设置进设置、Delete 删当前选中、录完进剪辑。需要非常规行为（例如关窗缩到托盘）必须**可关、可改默认**，并首次说清楚。拿不准时问：剪映或资源管理器会怎么做？按那个做。

---

## 三、核心工作流程（七步）

```
① 钉角色与目标 → ② 调查 → ③ 分级 → ④ 决策 → ⑤ 换端审查 → ⑥ 等确认 → ⑦ 执行 → ⑧ 验证 → ⑨ 回退 → ⑩ 留痕
```

**未先写角色/金路径/G/范围，禁止拉 PR 列表写「建议合并」。**  
方案未获确认前，**不改产品代码**。`12` 已拍板；动手仍须用户明示「开始做」。  
**执行中不中途提问。** 能自动的全自动。要人拍板的先代决写入 `14-待确认代决策与待检验.md` §二。合上后须真机看的写入 `14` §三。  
**综合考量过不去、且决策得出来（就是暂缓）**：先完整写入 `15-执行中暂缓与跳过.md`，立刻做下一条，禁止只在对话里提一句。人收工后先看 `15` + `09`，再按 `16-人工验收清单.md` 走查（项号关联 PR）。  
执行口径：`…/12-综合完善方案.md` §十。完整工作法：`docs/02-Areas/20260906-01-最佳实践_开源项目PR关注与合并.md` 与下文 §九。

1. **调查**：从源码、i18n 文案、GitHub（`gh`）挖掘行为与细节，一切结论以事实为依据。
2. **分级**：候选改动分 P0（保命）→ P1（提效）→ P2（按需）→ P3（不合并）。
3. **决策**：用综合决策模型打分（§四）。
4. **执行**：按优先级顺序实施（§五）。
5. **验证**：自动化测试 + 回归走查，**AI 尽量承担测试验证**（§七）。
6. **回退**：出错按「单 PR → 整批 → 版本」三级回退。
7. **留痕**：每个改动写进执行留痕文档（§六）。

---

## 四、综合决策模型（评价每个改动的风险/效能/贡献）

完整工具（闸门、量表、象限、极高分筛选、填卡模板）见  
`docs/02-Areas/20260906-01-最佳实践_开源项目PR关注与合并.md` **附录 A · PR 平衡矩阵**。  
**每个进入方案队列的 PR 必须过闸并填卡**，禁止只凭印象排序。

```
S = 0.40 × 风险R + 0.35 × 目标贡献C + 0.25 × 效能E   （各 1–5 分，5 分最好）
```

| 维度 | 权重 | 打分要点 |
|------|------|---------|
| 风险 R | 40% | 改动行数、是否动核心链路、CI 状态、是否 Slop（分高=更安全） |
| 目标贡献 C | 35% | 服务 G1–G4 的程度 |
| 效能 E | 25% | 解决你这个角色的高频痛点的程度 |

**闸门（先筛，不过不打分）**：范围 → 落进 G → 七问全过 → 非「>2000/巨型 Slop/换引擎」。

**决策阈值（仅过闸后）**：

| S 区间 | 动作 |
|--------|------|
| S ≥ 4.0 且在「优先」象限 | 进入优先池，再按附录 A.5 筛**极高分**（每轮默认最多 8 个） |
| 3.0 ≤ S < 4.0 | 👀 审阅后进后续队列 |
| 2.0 ≤ S < 3.0 | ⏸ 按需 |
| S < 2.0 | ❌ 不进方案队列 |

**极高分筛选（短规则）**：四闸全过 → 每集群只留主方案 → S≥4.00 且非「高价值高风险」象限 → 低垂三勾排最前 → 其余按 S/C/E/行数 → 金路径没修完时增强类不得挤进前 8。高价值高风险只进「手工」名单，AI 不自动开工。

---

## 五、执行顺序（优先级，不跳级）

1. **低垂果实**（同时满足：风险低 + 效果好 + 修复简单）——最先做
2. **核心路径问题**（影响录得稳/点得动/听得清/存得了/导出顺）
3. **功能增强**（最后考量，不主动扩张）
4. **功能增强改变了操作方式 → 必须同步使用手册**：创建/更新对应教程（`R20260906-01-Recordly用户使用指南/`），并明确提示用户「原操作方式已调整、新方式是什么、如何调整自己的操作行为」。

> ⚠️ **大而冒险的改动（>500 行、带 Slop、动核心链路）不在此列**：由开发者手工逐一操控，AI 只做前面较小、低风险的改动。

**每个改动合并前过闸门（七问，全过才做）**：

- ① 服务哪个目标(G1–G4)？② 是否不过度设计？③ 有无回归风险？
- ④ **用户视角·有必要吗？**（用户真的需要，还是「炫技」？培训师录课用得上吗？）
- ⑤ **用户视角·会带来困惑吗？**（新增入口/选项/开关，会不会让老用户找不到原来操作？）
- ⑥ **用户视角·会有奇怪影响吗？**（有无意外副作用、干扰现有操作、改变默认行为？）
- ⑦ **习惯对齐？**（默认是否符合大多数人的工作习惯、大多数软件的使用习惯？非常规行为是否可关？）

---

## 六、留痕规范（每次修改必须记录四要素）

| # | 要素 | 说明 |
|---|------|------|
| ① | 修改了哪些内容 | 具体文件、具体位置 |
| ② | 是怎么修改的 | 改法（可附 diff 要点） |
| ③ | 有什么作用 | 解决了什么、带来什么 |
| ④ | 对原工作流程的影响 | 是否改变录制/编辑/导出行为；**若改变操作方式，需同步更新使用手册并提示用户调整操作行为** |

> 落点：`docs/01-Projects/R…-调查分析/09-执行留痕.md`，每个改动一条。

---

## 七、测试与验证（AI 承担为主，降低开发者工作量）

- **自动化优先**：`npm test`（vitest，120 个测试文件）、`npm run lint`（biome）、`npm run typecheck`/`tsc`、`npm run i18n:check`。
- **回归走查**：录→停→编→导 整链路冒烟清单（启动→选窗口→录制→交互→停止→编辑→导出）。
- **AI 测试验证**：改动后由 AI 跑自动化测试、写/补测试、跑冒烟，**开发者只在最后做手工走查**。
- **回退**：单 PR `git revert`；整批 range revert；版本切回上一 tag。

---

## 八、PARA 落盘约定

<!-- BEGIN:para-structure-agents -->

### docs / PARA 落盘约定

`docs/`（或当前 PARA 根）下只有四维：`01-Projects`、`02-Areas`、`03-Resources`、`04-Archives`。

**按维分流，不要混用。** 创建或补全 PARA 目录时同步维护本锚点块，是推荐做法。

| 维 | 放什么 | 是否建主题子文件夹 |
|----|--------|-------------------|
| `01-Projects` | 每次输入的**需求、方案**及同主题配套稿（任务、调查、改善报告等） | **要**：一个主题一个项目夹 |
| `02-Areas` | 长期维护的规范、领域笔记、调查备忘（无明确交付闭环） | **不要**：文件直接落在该维根下 |
| `03-Resources` | 外部参考、剪藏 | **不要** |
| `04-Archives` | 已结束项目整体迁入 | **不要**为新主题 mkdir；迁入时保持原项目夹 |

### `01-Projects`：需求 / 方案按主题夹

落到 `01-Projects/<项目夹>/`，不要散落到另外三维。

```text
R{YYYYMMDD}-xx-主题
```

- `{YYYYMMDD}`：创建日；`xx` 为当日已有同前缀夹的下一号（从 `01` 起）
- 夹内用 `00-README.md`、`01-需求_….md`、`02-方案_….md` 等编号文件，不要再套一层主题子目录
- 已有同主题夹则追加文件，不要平行再开一夹
- 新建夹优先：`para-structure cp "主题"`

### 另外三维：禁止再嵌套主题子文件夹

`02-Areas`、`03-Resources`、`04-Archives` 的根下直接放 `YYYYMMDD-xx-主题.md`（序号按该维根下当天已有同前缀文件计）。

| 不要 | 要 |
|------|----|
| `02-Areas/主题/需求.md` 或 `02-Areas/YYYYMMDD-xx-主题-需求.md` | `01-Projects/R…-主题/01-需求_….md` |
| 在 Resources / Archives 根下为新主题 mkdir | 单文件落在该维根下（归档迁入的整夹除外） |

`.gitkeep` 与 PARA 根 `README.md` 是脚手架，不要删。

<!-- END:para-structure-agents -->

---

## 九、开源 PR 采纳工作法（智能体必做）

> 人用的长文在 `docs/02-Areas/20260906-01-最佳实践_开源项目PR关注与合并.md`。这里只留**执行时不可省的步骤**。目标是把开源项目改造成符合**本仓库主人**的工作流，不是替上游当维护者。

### 9.1 先钉再看（缺一项就停）

动手调查 PR 之前，必须能在对话或 `00-诉求` / 本文 §一 里指出：

1. **角色**（一句，禁止「用户」这种空词）
2. **用途 / 金路径**（一串动词）
3. **为什么要改这个软件**（缺的是哪一段，不是「看起来酷」）
4. **G1–G4 或等价可衡量目标**
5. **范围**：平台、语言、工作分支、不代合/不代关上游

后说的范围覆盖先说的。落不进 G 的 PR 不合。

### 9.2 关注什么、优先什么

**盯**：金路径断裂；点名能力；默认行为会不会被换掉；行数/Slop/CI；是否出范围；重复与依赖；操作方式变不变。  
**不盯**：替上游关 PR、其他 OS/语种凑热闹、与金路径无关的热度。

优先级不跳级：

1. 低垂果实（风险低 + 效果好 + 修复简单）
2. 金路径断裂（点得动 / 听得清 / 存得了 / 编得动 / 导得出）
3. 点名能力（过大则暂缓、记下方向）
4. 功能增强（最后，默认「可选」，不改老路径）

打开每个候选 PR 只看：症状是否在金路径上 → 平台/语言 → diff 规模 → 默认是否变 → 是否替换引擎/后端 → 标签 → 属于哪个集群。  
进入队列前按附录 A 填平衡卡；只对「极高分」名单建议按序做。

### 9.3 历史提示是诉求真源

- 导出**当前工作空间**的历史提示，压成约束并对账；不要靠聊天记忆，不要把别的仓的提示并进决策。
- 方案必须能回答提示词里的每一条（含安装包、身份、教程，不只是代码 PR）。
- 早期调查稿与后来提示冲突时：**以提示词时间线为准**，旧稿降为底稿并声明真源。

### 9.4 换端审查再拍板（禁止自审自过）

写出综合方案后：

1. 换另一端智能体（或新开无同一套未检验假设的会话）审查。
2. 审查方必须先读历史提示与角色/G，再改决策，禁止只润色措辞。
3. 重点查：漏诉求、自相矛盾、维护者心态、范围外混进队列、七问没过、习惯是否偏离大多数软件、暂缓未留痕。
4. 改回**一份**方案真源，等人确认。**审查完不得直接改产品代码。**
5. 两端打架时回到角色和金路径，不回到「哪个模型更自信」。

### 9.5 做的时候

- 本地分支移植（cherry-pick / 手工），一个一验。
- 能共存则「默认保原体验 + 可选新能力」；真互斥默认不采用。
- AI 只做 ≤500 行低风险；更大 / Slop / 动核心交给人。
- 改了操作 → 同步使用手册并写变更提示。
- 留痕四要素；暂不采用也写「将来何时再看」。
- **执行中不提问**：选择题先代决写入 `14` §二；真机检验写入 `14` §三。过不去且能决定暂缓 → **先记 `15-执行中暂缓与跳过.md`（写全原因、试过什么、下一条是谁）再跳**。只有「开始做」和「连续金路径失败暂停」才等人。
